# #실시간 current release state

Updated: 2026-07-24
Working branch: `agent/silsigan-backend-finish-20260724`; upstream: `origin/codex/silsigan-progress-20260710`
Verified local source/evidence commit: `d35607ab2b4a4a8b8418355d24226c7d99e18585`.
Working-tree status after the candidate commit: clean before this documentation-only release record. The 2026-07-24 photo-location security remediation, staging deployment, Computer Use flow, safe-tail, directory-failover, and public-source artifacts are candidate evidence. Verify any later record commit and remote branch tip separately before release use.
Commit/push gate: manual source/diff security gate, high-confidence secret scan, same-day dependency audit, and negative-path regressions pass. The current Codex Security scan of `e760dd5` reported one Medium CWE-345 client-location trust finding; source `d35607ab2b4a4a8b8418355d24226c7d99e18585` requires server-validated client-location evidence, binds only coarse radius/accuracy claims into upload tickets, removes server-endorsed physical-location wording and internal identifiers from public photo responses, and adds missing-location/privacy regressions. The earlier cache-invalidation finding remains separately remediated at `656eb315cbde4505b6c7db342a0185bb2762baea`.

## Objective

기존 Supabase 기반 지역 MVP를 Cloudflare Workers/D1/R2/Durable Objects 기반 전국 실시간 장소 앱으로 전환한다. 완료 판정은 로컬 테스트 통과가 아니라 Cloudflare staging/production 리소스와 운영 증적까지 분리 검증된 상태를 기준으로 한다.

Current release target: App Store production submission is not the next milestone. The current milestone is a Cloudflare-backed TestFlight MVP with live staging URLs, R2/Images evidence, staging smoke, and real-device QA. See `docs/testflight-readiness.md`.

Next unblocked operator packet: `docs/cloudflare-staging-operator-packet.md`.

Integrated completion plan: [silsigan-v2-master-completion-plan-2026-07-21.md](silsigan-v2-master-completion-plan-2026-07-21.md).

V2 completion contract: [v2-decision-register.md](v2-decision-register.md). The 11 product, platform, privacy, source-rights, and mobile decisions are fixed there. Local implementation is complete for all 11 decisions, including server-bound anonymous proof lifecycle and the optional member-link seam; external console, rights, migration, deployment, legal, and real-device evidence remains separate and is not represented as complete.

## Latest external update — 2026-07-24

This section supersedes older 2026-07-22 Cloudflare statements retained below as historical evidence.

Active truth summary: root tests `462/462`, mobile tests `4/4`, same-day dependency audits `0` known vulnerabilities with no lockfile change, staging API version `3f1d64bc-c62b-4788-9b76-173b210a242f`, staging web version `2483c542-e10d-458b-9f40-12b835d80f62`, live API/browser read-only smoke pass, private staging R2, staging D1 through `0026`, home-to-upload and current-location auto-link Computer Use pass, the human Turnstile checkbox awaiting explicit confirmation, zero D1/R2 upload residue, and production unchanged.

- Security-first photo-location source `d35607ab2b4a4a8b8418355d24226c7d99e18585` requires browser-provided coordinates before any Turnstile, D1, or R2 photo-ticket work. The server rejects missing location with `PHOTO_LOCATION_REQUIRED`, checks accuracy and place radius, stores no exact photo coordinate, exposes only a coarse client-reported proximity result, and explicitly states that this is not physical-presence or identity proof.
- `pnpm verify:app` passes lint, typecheck, root tests `462/462`, and Next.js production build. `pnpm verify:workspaces` passes mobile lint/typecheck/tests `4/4` plus WebView syntax. Clean OpenNext staging/production web/API Wrangler dry-runs pass; the production checks were dry-runs only.
- Staging API version `3f1d64bc-c62b-4788-9b76-173b210a242f` and staging web version `2483c542-e10d-458b-9f40-12b835d80f62` are deployed from the remediated source. The final web bundle embeds the exact public staging API base. An interim fail-closed bundle that omitted that public base was detected by Computer Use and replaced before the upload flow.
- Post-deploy read-only smoke passed health, 14 places, place detail/status, three realtime scopes, rankings, photos, and deny-by-default admin `403`. Response headers retain HSTS, CSP, restrictive geolocation policy, `nosniff`, and frame denial.
- Computer Use opened `올리기` directly from Home, used an explicit QA-only Chrome location emulation at 광안리 after the Mac location was too imprecise, auto-linked `광안리해수욕장` without manual place selection, rendered the real NAVER map, confirmed the rights attestation, and selected a bounded JPEG. This proves the browser interaction path only; the emulated coordinate is not real field-presence evidence.
- The exact-host managed Turnstile now presents its visible `사람인지 확인하십시오` checkbox. Automation did not click or bypass the human challenge. No upload success is claimed. A post-selection read-only reconciliation still reports active photos `0`, active photo bytes `0`, consumed upload claims `0`, pending cleanup jobs `0`, and staging R2 object count/size `0`/`0 B`.
- The same-day root/mobile dependency audit reported `0` known vulnerabilities and the dependency/lock state did not change. A later fresh metadata fetch was denied by the environment's outbound dependency-metadata policy, so the already-completed same-day audit is retained rather than misreported as newly refreshed.
- Production remains intentionally blocked and unchanged: no production Worker, R2, D1 migration, secret, provider activation, or traffic mutation was performed.

- Next.js and `eslint-config-next` were patched from `16.2.6` to `16.2.11`; root and mobile PostCSS overrides were patched from `8.5.10` to `8.5.12`. Root and mobile low-severity dependency audits now report no known vulnerabilities.
- Current split verification passes root lint/typecheck/tests `462/462`, Next.js `16.2.11` production build, mobile lint/typecheck/tests `4/4`, WebView syntax checks, OpenNext build, and staging/production web/API Wrangler dry-runs.
- Final staging web version `2483c542-e10d-458b-9f40-12b835d80f62` was built with the exact staging API base and deployed. Post-deploy browser smoke passed home/map/search/detail, live Worker place/photo/realtime paths, and the upload entry/current-location path.
- Read-only staging API smoke passed health, 14 places, place detail/status, place/region/global realtime rooms, rankings, empty photo/comment lists, and deny-by-default admin `403`.
- A raw Wrangler tail capture revealed that platform tail events include request headers, query strings, and exact Cloudflare geolocation even when application logs are clean. That raw file was immediately deleted. The new bounded capture accepts only the exact staging API Worker, sanitizes platform metadata and request paths in memory, preserves Worker logs so application leaks still fail, detects R2 object keys, writes with mode `0600`, and refuses existing/unsafe output. Its final 20-second live run captured 20 events, processed 88,840 raw bytes in memory, persisted 10,688 sanitized bytes, and passed with no sensitive findings.
- Private staging R2 and D1-through-`0026` read-only evidence remain green. The static directory failover browser harness again passed with no API, provider, or analytics calls after fallback; official source fixtures passed all six fail-closed scenarios while every live provider target remains disabled.
- Production remains intentionally blocked: `silsigan-photos-production` and the production API Worker are absent, the production API URL is unset, and production D1 first becomes incomplete at `0006`. No production resource, migration, secret, provider activation, or traffic was changed.

## Historical external update — 2026-07-22

This section supersedes older same-day Cloudflare statements retained below as historical evidence.

Active truth summary: root tests `454/454`, mobile tests `4/4`, staging API/web live after the Workers KV read-remediation deployment, private staging R2 `0 B`, staging D1 through `0026`, human Turnstile photo lifecycle pending, and production unchanged. Local, staging, production, device, and human evidence remain separate.

- Staging R2 is active with private bucket `silsigan-photos-staging`; `r2.dev` is disabled, no direct R2 custom domain is attached, incomplete multipart uploads abort after seven days, and `photos/_uploads/` objects expire after one day.
- Staging D1 is aligned through `0026` with no pending migration. Production D1 remains separately blocked and was not changed.
- Staging API version `dc07bf4a-879a-421c-aea1-5418f9c8bc0e` is deployed at `https://silsigan-api-staging.dudqks0319.workers.dev`. It reuses the cost-guard control row loaded before authentication, avoiding a second `COST_GUARD_STATE` KV read for each admitted public request. D1 remains the authoritative atomic reservation ledger. Production was untouched.
- Staging web version `ad980039-5ba0-4ffb-9616-6f60eb6aea54` is deployed at `https://silsigan-web-staging.dudqks0319.workers.dev` with the exact staging API base embedded at build time. After initial load it refreshes only while visible, elects one same-origin leader, prevents overlap, runs every 60 seconds, and limits the refresh fan-out to places plus at most five statuses.
- The cache-busted staging browser smoke passed the full non-mutating home/map/search/detail path with 65 API requests against a hard regression budget of 80.
- The first immediate post-deploy smoke received one transient stale HTML document and failed on a removed chunk. The exact root then returned the current chunk on three consecutive reads and the release smoke passed. This failure remains recorded as rollout evidence.
- Staging now loads real NAVER styles/tiles from `nrbe.pstatic.net` and `ssl.pstatic.net/static/maps`. A false resource-failure fallback caused by legacy-host-only detection is fixed; after five seconds the real NAVER map, logo/legal assets, and place markers remained present.
- Photo writes were enabled only after live R2 `0 B` and zero active D1 photo bytes were reconciled. On 2026-07-22 a valid bounded JPEG was selected through the real staging web picker and reached the exact-host managed Turnstile challenge. The automation environment was rejected before token issuance, so no successful upload or R2 object is claimed and the moderate/read/delete zero-residual smoke remains pending on a human Turnstile check. The widget remains managed and is not weakened.
- The two unattended staging app tabs responsible for repeated polling were closed. The exposed Turnstile secret was rotated in Cloudflare and the replacement was piped directly into the staging Wrangler secret without printing or writing its value. Cloudflare may accept the retired value for a rotation grace period of up to two hours. Temporary admin-token and photo-smoke files were deleted. Production secrets were not touched.
- Codex Security completed a 15/15-file diff review of `d7f0499..f5abc50`. It found one Medium same-client stale-UGC path after creator block/moderation. Source `656eb315cbde4505b6c7db342a0185bb2762baea` now reserves the low-cost `places_status` scope for four map/background callers, forces security-sensitive mutations through full reconciliation before success, clears account-owned caches synchronously, and adds focused regressions. The original non-mutating PoC no longer matches the fixed source, and an independent re-review found no remaining actionable issue in the scoped fix.
- Before remediation, the two tabs produced about `176 KV reads/min` at idle (`2 × 22 requests × 2 ticks/min × 2 reads`). The new two-tab design ceiling is about `6 KV reads/min` (`1 leader × 6 requests/min × 1 read`), excluding initial load, cron, and user actions, a reduction of about `96.6%` for that scenario. The dashboard's rolling 24-hour `0.9 reads/s` and `63.9k reads` observation includes pre-fix history and is not immediate post-fix slope proof; recheck after the `09:00 KST` daily reset.
- The API guard first entered `degraded` mode with reason `automatic-70-percent-d1_rows_read`; a later conservative write reservation also triggered the same fail-closed 70% boundary. Cloudflare showed about 87,870 actual rows read for the earlier visible billing period, while the application ledger had reserved exactly 3,500,000 rows from 1,583 admitted requests; the heaviest observed public query averaged 59 rows. Route weights are calibrated to 100 rows for standard/essential reads, 500 for high-cost reads, 200 for personal reads, and at most 500 for writes. The latest fresh observations are Workers `3,000`, D1 rows read `504,922`, and rows written `37,386`; the audited path resumed generation `5` without direct D1 edits.
- Public source ingestion targets and enabled targets remain zero. KMA, TourAPI, and national-parking credentials are installed as staging secrets only; ITS remains unissued. A post-install read-only D1 check confirmed all six official provider rows remain disabled (`user_report` is the separate enabled UGC source). Source rights, attribution, quota, TTL, health, region, and fallback approval remain required before any activation. Each provider is additionally protected by a server-side Korea Standard Time daily request budget checked before outbound network access, a `0` kill switch, compiled ceilings, fail-closed timestamp handling, and at most two reserved attempts; ITS traffic and CCTV share one budget.
- Production API deployment, production migration, provider-source activation, custom owner domain, real-device QA, and named legal/operations sign-off remain blocked. No production traffic was changed.

## Integrated candidate update (2026-07-21)

- Source candidate `48e672aa3f9ece06e31f40305fc6e080c69fb479` merges the Mac mini `5055e62` baseline with one shared 1 MiB final-photo contract, a 12 MiB local-source ceiling, bounded browser re-encoding, a 20 MiB/IP-fingerprint/day upload-byte guard, updated dependency locks, and the role-by-role completion plan.
- The current root test set passes `454/454`; lint and typecheck pass. The full build, workspace verification, clean Cloudflare staging/production web dry-runs, dependency audits, and secret scan also pass. The verified source SHA is `656eb315cbde4505b6c7db342a0185bb2762baea`; the remote branch tip still requires post-push equality verification.
- Root and mobile dependency audits report no known vulnerability. The final 345-file tracked/non-ignored source scan and full Git-history high-confidence check for the retired Turnstile-key shape found no hit; non-empty sensitive assignment checks also found no file. `.env.example` contains empty secret placeholders only.
- GitHub Actions run `29788726774` passed the deterministic verify job and the Android JDK 21 lint/debug-shell build. Its annotations are upstream Actions v4 Node 20 deprecation notices forced to Node 24 by the hosted runner, not test failures.
- `pnpm release:status` correctly remains `blocked-external` because the release ledger still has open photo, WAF, provider-rights, device, legal, owner-domain, and production gates. The recovered staging API does not make production release URLs or traffic approved.
- The earlier 2026-07-21 non-mutating check reported `R2_NOT_ENABLED` and an unhealthy staging API. Those staging blockers were subsequently resolved as recorded in the latest external update above. Production API deployment and production `D1_0006_NOT_APPLIED` remain open.
- No production deployment, remote production migration, provider application submission, payment/terms acceptance, or traffic promotion was performed.

## V2 Re-audit Closeout (2026-07-14)

Checked items below are local code/test evidence. Unchecked items require Cloudflare, provider-console, device, legal, or named-operator evidence; they do not become complete from a local build.

### Local implementation

- [x] Worker direct API feature gates are enforced for Q&A, social feed, hashtags, live streams, and ads, including regional flags.
- [x] Client-supplied question credit balances are ignored; Q&A/rewards fail closed until a server credit ledger is available.
- [x] `LiveSignal.expiresAt` is required, legacy rows are backfilled by migration `0007`, and missing expiry is excluded from current UI/API truth.
- [x] Field-report location verification uses server-owned point/radius or polygon/multipolygon checks plus coarse accuracy buckets; low or missing accuracy falls back to an unverified report, raw accuracy is not persisted, and migrations `0011_location_accuracy_buckets.sql` and `0012_field_verification_method.sql` store only coarse verification metadata.
- [x] New field reports start as `pending`; public reports, votes, live signals, place status, ranking live score, and realtime events exclude unapproved rows, while `mine=1` exposes the submitter's moderation status. Approval atomically updates the public event/signal/aggregate/publication/audit state and deterministic outbox, whose consumer rechecks approval before broadcast. Moderator queue/action routes and the Next admin field-report queue write `admin_actions` through migration `0013_field_report_moderation.sql`.
- [x] Haversine distance and distance-aware nearby sorting are local-only; raw coordinates are excluded from analytics payloads.
- [x] Placeholder trust metrics and expired hard-coded campaigns are not shown as current truth; legacy campaign/quest fixtures are additionally fail-closed behind `REWARDS_ENABLED`/`QNA_ENABLED`.
- [x] Missing place status and observation-free report cards use a neutral `unknown` tone instead of the visual `normal` state.
- [x] Share URLs are HTTPS-only and environment-overridable through `SILSIGAN_PUBLIC_SITE_URL`.
- [x] 전국 지도·검색은 모든 지역에서 제공하되 최신 근거 없는 장소를 순위에 넣지 않는다. 장소 딥링크, 딥링크 URL 정리, 원래 화면/스크롤 복귀, 실제 승인 제보 전용 공유, sample 시각 구분, semantic tab/키보드 이동을 2026-07-20 Chrome으로 재검증했다.
- [x] 일반 `올리기` 진입은 탐색 중인 장소를 암묵적으로 재사용하지 않는다. 사용자가 검색·지도·장소 후보에서 대상을 명시적으로 고르기 전에는 사진·상태를 어떤 장소에도 연결하지 않으며, 로컬 브라우저 하네스가 선택 전 차단과 선택 후 제보 발행을 검증한다.
- [x] iOS/Android 네이티브 사진 선택은 HEIC/HEIF를 bounded JPEG로 변환하고 MIME 선언뿐 아니라 JPEG magic byte까지 확인한다. 변환을 증명하지 못하거나 forged JPEG MIME이면 업로드 전에 실패 폐쇄한다. 실제 iPhone/Android 사진 보관함·카메라 증거는 real-device QA에 남아 있다.
- [x] Anonymous saved places/posts, followed topics, and notification subscription hashes have D1 migration `0008` and API sync paths; native push registration forwards platform metadata while the client stores/transmits only a SHA-256 token hash, and opt-out clears the stored hash.
- [x] Local `0023_anonymous_session_proofs.sql` and the shared API client replace the forgeable ID-only write identity with a server-issued 256-bit proof whose ID and proof are stored only as SHA-256 hashes in D1. Local `0024_anonymous_session_cost_guard.sql` atomically caps issuance across POPs at a compiled maximum of 5,000 per UTC day, accepts a lower value including `0` as a kill switch, fails before session insert when its ledger is absent, and lets the five-minute Cron prune expired/old-revoked sessions and old budget rows. Public `GET/HEAD` requests skip session issuance, so exhausting that budget cannot disable nationwide place, map, or approved-photo reads; writes and private reads lazily require the proof, and both the Worker and browser client are tested after issuance-budget exhaustion. Staging/production require the proof, the browser keeps the credential in `silsigan.anonymousSession.v2`, account deletion clears it, and the Worker/harness reject a stolen ID without its proof, an incorrect or rotated proof, and a revoked proof. A dedicated Cloudflare binding reduces first-line requests at 3/minute per hashed IP before D1, while the D1 reservation is the exact global boundary; authenticated requests refresh `last_seen_at` at most once per 24 hours. Live owner-domain deployment evidence remains external, and compromise of the complete ID+proof pair remains a bearer-token risk.
- [x] Local `0025_place_addition_requests.sql` and the web/admin flows keep NAVER external-search results display-only and accept only a separate manual name/address/category request. The queue is owner-only, omits internal review reason from owner reads, replays idempotently, limits each anonymous session to 3 requests per UTC day, stops globally at 1,600/day, and lets moderators classify `needs_verification`, `ready_for_manual_import`, `duplicate`, or `rejected` without automatically inserting a public place. Duplicate classification requires an existing verified place and every review is audited.
- [x] Local `0026_global_api_cost_guard.sql` reserves conservative Workers/D1 route weights before guarded work, accepts higher Cloudflare-observed values, sends one redacted 60% warning, degrades non-essential work at 70%, preserves only privacy/emergency critical writes to 80%, then stops. Nationwide essential reads fall back to a D1-write-free snapshot. Health checks use the public edge limiter and global ledger; forged anonymous proofs reserve authentication lookup cost before D1; each admin route verifies its exact minimum role before protected-capacity reservation; and resume freshness/usage is rechecked atomically inside the state-change transaction. The same-origin admin panel supports immediate stop, three-meter reconciliation, and resume only with observations no older than 15 minutes and below 70%.
- [x] Production analytics sink, allowlisted events, server redaction, and hashed anonymous actor keys have migration `0009`.
- [x] The required analytics event contract is wired through the client and Worker allowlist, including location permission, nearby load, map success/failure, report verification/submission timing, moderation, content reports, runtime errors, stream, and ad events; production delivery still requires external sink evidence.
- [x] The operator-only `/api/admin/beta-kpis` endpoint and same-origin Next admin panel provide 7/30-day aggregate audience, funnel, map reliability, moderation SLA, D1/D7 retention, fresh coverage, and web runtime reliability. Responses contain no actor identifiers or event properties, and rejection rate is explicitly not treated as a proven false-report rate; live beta data and native crash-free evidence remain external.
- [x] Fresh-evidence coverage is split into aggregate, Tier A (`launch_stage=active`), and Tier B (`launch_stage=beta`) cohorts. The panel exposes the Tier A 70% launch threshold without turning Tier A/B into a regional feature lock; every region keeps nationwide search, map, and reporting access.
- [x] Seoul realtime city-data adapter normalizes provider area identity, estimated crowd level, observation time, bounded expiry, attribution, and rejects unknown provider labels; `SEOUL_REALTIME_ENABLED` and source rights/health activation remain disabled.
- [x] Local migration `0018_source_ingestion_scheduler.sql` and the Worker `scheduled()` handler add disabled-by-default targets, a 10-target bounded batch, four-minute conditional leases with completion fencing, per-target backoff/failure isolation, KMA publication-time resolution, and aggregate-only logs. Local `0019_background_job_delivery.sql` makes photo cleanup and publication outbox delivery leased, fenced, retry-bounded, dead-lettered, and storage-budget-aware. Staging and production both dispatch the internal consumers every five minutes, while `SILSIGAN_SOURCE_INGESTION_SCHEDULED=0` prevents production from calling external providers. No target is seeded or enabled, and `docs/source-ingestion-scheduler-runbook.md` keeps rights/credential/health/quota review ahead of activation.
- [x] Next.js applies a route-wide restricted CSP plus anti-clickjacking, no-sniff, HSTS, referrer, permissions, and cross-domain-policy headers. The policy preserves same-origin camera/location for the native WebView flow, disables microphone access, and is present in the production route manifest.
- [x] Capacitor iOS/Android project skeletons were generated locally from `apps/webview` with app id `kr.silsigan.mobile`; iOS camera/photo/location usage descriptions and target-root `PrivacyInfo.xcprivacy`, Android location/camera/notification permissions, cleartext blocking, app-data backup/device-transfer exclusions, restricted FileProvider paths, and the `SilsiganShell.openSettings` adapter are declared/implemented. `docs/store-privacy-disclosure-draft.md` records the App Privacy/Data Safety inventory and is guarded by `release:status`. On 2026-07-19 the actual staging URL/origin was synchronized into both native shells; Android lint/assemble passed with APK SHA-256 `7439ae76464271f4dd26ae1ef7c4b5753b92ea52d49811b8d12244e53c4ae90f`, and an unsigned generic iOS Simulator build packaged the exact source privacy manifest at the app-bundle root. Signing, push delivery, final archive privacy report, store-console answers, named legal review, and real-device evidence remain external.
- [x] Binary photo upload-ticket/upload flow, metadata sanitization, R2 deletion, retry cleanup queue, atomic storage/write/Images-transform/read ledgers, signed one-use tickets, HMAC IP daily budget, and upload/read emergency controls have migrations `0009`/`0010`/`0015`/`0016`/`0017`/`0020`/`0021`/`0022` and local tests. `0022_photo_storage_release_ledger.sql` fences every byte release by storage key/token, reconciles legacy rows conservatively, and disables uploads until an operator compares D1 with live R2. Automatic stops are about 3.2 GiB storage, 12,800 monthly writes, 4,000 monthly Images transformations, 800,000 monthly Class B reads, and 16,000 daily D1-tracked reads; configuration may lower but not raise the compiled caps.
- [x] Shared frontend API client uses the same upload-ticket/multipart binary contract; the legacy Korean UX fixture surface has no static social, Q&A, or expired campaign content.
- [x] Social-feed disabled state is consistent in the frontend: posts/hashtags requests are skipped and post cards expose report evidence rather than `post.judgement`.
- [x] Truth-bearing UI and Worker share/OG responses use V2 aggregated place status; individual post values are labeled as report content, and missing current status renders `현재 정보 부족`.
- [x] Home official weather is scoped to the currently visible places, prefers the nearest visible place order, and ignores stale/expired statuses from a previously selected region.
- [x] The separate Expo mobile shell passes its local `pnpm --dir apps/mobile verify` check (lint, typecheck, and 4/4 tests); this does not claim Capacitor native project, signing, or real-device readiness.

### Remaining local scope (not falsely checked)

- [~] Field-report verification is server-owned point/radius or polygon/multipolygon validation with coarse accuracy and rate limits. Browser/WebView mock-location attestation is not available, so no complete spoofing-prevention guarantee is claimed.
- [x] The current app/client no longer uses base64 photo upload. Legacy `/api/photos/upload-url` and `/api/photos/complete` remain temporarily for compatibility and return `Deprecation: true`, a successor `Link`, and `x-silsigan-photo-contract: legacy`; staging access-log evidence is still required before removal.

### External release blockers

- [x] Local `0014_field_report_publications.sql` links same-user same-place photos, normalized hashtags, idempotency keys, and an outbox; pending publications do not enter public hashtag counts.
- [x] After the protected backup and local restore drill, the guarded recovery path preserved the empty legacy table and normal Wrangler migrations applied `0018`~`0026` to Staging. The latest schema-only boundary passes through `0026`; Wrangler reports no pending migrations, and migration registry plus core seed evidence agree.
- [~] A mode-`0600` pre-change Staging export with a recorded SHA-256 remains outside the repository. Preserve it and rerun non-mutating D1 evidence before deployment. Separate staging/production `COST_GUARD_STATE` namespaces are provisioned and bound. Because `0022` is present, recorded live R2/D1 reconciliation is required before uploads resume and the API Worker deploys.
- [ ] Production D1 remotely applies and verifies the full V2 chain through `0026` after clean staging evidence and separate approval; its current first failure is `D1_0006_NOT_APPLIED`.
- [ ] R2 account subscription, buckets, Images path, public/private object behavior, and deletion evidence.
- [ ] Staging API Worker, Pages/API HTTPS URLs, `/api/health`, `/api/places`, `dataMode=live`, and zero demo responses.
- [ ] Official source keys/rights/attribution, real ingestion, staging `observedAt`/`expiresAt`, and health logs. Local fixture fallback evidence now passes all six adapters with bounded stale data, expired `insufficient`, and no sensitive output, but does not satisfy this external gate.
- [~] NAVER Dynamic Map Application, public Client ID, localhost real-tile success, daily `160,000`/monthly `4,800,000` hard limits, over-limit denial, daily/monthly `70%` alerts, and staging protection fallback are verified. Owner-controlled custom domain, representative-account evidence, an actual alert recipient, valid-origin success, and invalid-origin rejection remain.
- [~] Real staging admin roles and audit denial boundaries are verified, and bounded Workers tail redaction passed on 2026-07-24. `MODERATION_ALERT_WEBHOOK_URL`, named on-call ownership, and the live mutation/moderation lifecycle remain open.
- [ ] Native release and device evidence: staging URL injection, simulator evidence, and a 2026-07-23 Apple Development-signed iPhone 12 Pro build/install/launch/startup-console smoke pass. Screenshot-backed iPhone permission/photo/user-flow QA, Android real-device QA, TestFlight release signing/archive privacy report, and push delivery remain.
- [ ] Production analytics/notification delivery evidence, Capacitor iOS/Android device QA, legal sign-off, TestFlight, and an actual staging rollback/forward-restoration drill. Read-only staging web candidate discovery now passes but does not replace the live drill.
- [ ] Staging `0023`/`0024` schema is applied; deploy the proof-required Worker with `SILSIGAN_ANON_SESSION_DAILY_LIMIT=5000` on the selected owner domain, then capture live rotation, revocation, wrong-proof, stolen-ID-without-proof, observed edge throttling, and exact D1 budget evidence before broad public launch.

Production promotion remains prohibited until every external blocker is checked.

## Local Code State

| Area | State | Evidence |
| --- | --- | --- |
| V2 visit-decision UX | ready-local | Home now prioritizes `지금 방문해도 되는가`, shows source/observed time/confidence/expiry, keeps five-place list fallback beside map recovery, starts field reports with no preselected observation, and allows a general report when location is denied. Q&A, rewards, ads, live streams, social feed, and demo data default off; Seoul realtime remains flag- and rights-gated. |
| V2 decision engine and identity | ready-local-and-staging-schema, live-proof-pending | Shared contracts enforce dimension TTLs, expiry, conflict handling, independent-source consensus, `insufficient` for missing information, anonymous/member namespace separation, signed member-link seam, blocks, report votes, and account deletion. Staging now includes `0023` server-bound proofs and `0024` exact D1-wide 5,000/day issuance budget. A separate 3/minute hashed-IP edge binding reduces abusive issuance before D1. API deployment, owner-domain live evidence, and external member authentication remain missing. |
| V2 source registry and adapters | ready-local-and-staging-schema, activation-blocked | KMA, TourAPI, national parking standard, ITS traffic, ITS CCTV, and Seoul realtime adapters validate provider shapes through a redacted retry/cache/circuit gateway. Static sources cannot become current signals; CCTV stream URLs are dropped and playback remains disabled. Staging now has the inert, bounded, lease-fenced `0018` scheduler schema with zero unsafe active targets. Production external source ingestion is fixed off. The rights snapshot and activation/rollback runbook remain subject to named legal review, credentials, quota, health, TTL, and audited activation. |
| V2 D1 migrations | local-and-staging-through-0026, production-pending | Additive migrations through local `0026_global_api_cost_guard.sql` pass the SQLite chain tests. The latest read-only account check verifies Staging through `0026`, no pending Wrangler migrations, aligned registry, and core seed evidence. A protected pre-change external backup exists. Production remains pending separate approval. |
| Cloudflare rollback harness | ready-readonly, live-drill-pending | `pnpm cf:rollback:drill` is plan-only by default, rejects mutating flags before command execution, and optionally reads only `wrangler deployments list --json`. The 2026-07-20 staging web check found a distinct stable previous version and wrote redacted evidence without changing traffic. Actual rollback, smoke verification, and forward restoration remain approval-gated; Worker rollback does not restore D1, R2, KV, Durable Objects, migrations, or secrets. |
| Capacitor WebView shell | ready-local, partial-ios-real-device | `apps/webview`, app id `kr.silsigan.mobile`, generated `ios/` and `android/` projects, iOS permission descriptions plus target-root `PrivacyInfo.xcprivacy`, Android permissions plus deny-by-default backup/device-transfer rules and app-scoped FileProvider paths, cleartext blocking, registered `SilsiganShell.openSettings` adapter, exact HTTPS origin navigation, system-browser external links, schema-validated bridge, coarse-only native callback, camera/library/share/network/keyboard/back/deep-link seams, privacy-safe native push token forwarding, and deployed Next bootstrap are implemented. Fresh Android lint/debug build and simulator evidence remain valid. On 2026-07-23 an Apple Development-signed arm64 Debug build was installed and launched on an iPhone 12 Pro / iOS 17.6.1, reached the exact staging URL, and remained alive through a bounded `WebView loaded` console observation. Screenshot-backed permission/photo/user-flow QA, Android real-device QA, signed archive privacy report, store-console answers, push-provider delivery, and named legal review remain external work. |
| Photo privacy and moderation | ready-local, staging-r2-private, human-smoke-pending | Original files are not retained; binary upload-ticket flow removes metadata, re-encodes pixels, excludes original filenames, rejects exact duplicates, keeps images pending until moderation approval, deletes R2 objects, and queues failed cleanup. R2/D1 `0 B` reconciliation and audited write resume passed; human Turnstile upload/moderate/read/delete and zero-residual proof remain pending. |
| Next.js frontend | ready-local | 지도 SDK 인증/타일 실패 시 fallback 유지, SDK ready 후 빈 지도 방지, 모바일 지도 컨트롤 클릭 영역 보정, Haversine 기반 실제 거리 정렬, 만료 캠페인 fail-closed, 정보 부족/관측 없는 제보 카드의 neutral `unknown` tone, placeholder 신뢰도 제거, V2 aggregated status truth, feature flag 일치형 posts/hashtags fetch, report-evidence-only feed card, Worker-backed 장소/게시물/해시태그/질문 read path, status-aware share/OG lookup, 선택 지역 scoped fetch, persistent preference sync, analytics event sink, 전국 최신 근거 TOP 10의 evidence-only 랭킹, 장소 딥링크와 URL 정리, 원래 화면/스크롤 복귀, 승인된 실제 제보 전용 공유, sample 시각 구분, semantic/keyboard tabs, Worker 사진 place-scoped fetch/preview/click 동기화, 상세 시트, 홈 피드 탭 필터, 팔로우 기반 알림 토글, Worker 좋아요/장소·댓글·사진 신고 동기화, HTTP snapshot + WebSocket-first + bounded fallback polling UI smoke 증적은 `docs/security-gate.md`에 기록. OpenNext Cloudflare adapter config와 pinned dependency가 있으며 `pnpm cf:typegen`은 `.env.example` 기반으로 생성되고 `pnpm cf:build`가 `.open-next/worker.js`와 `.open-next/assets`를 생성한다 |
| Vercel preview | ready-preview, backend-blocked | 2026-07-11 project framework was corrected from `Other` to `Next.js`; protected deployment `dpl_82KfB9KGLrpysFVRjGnr36qDHnHN` is `Ready`, authenticated home returns `200`, and `/api/places` returns the intended `503 LIVE_BACKEND_REQUIRED`. Production alias was not changed because Cloudflare API Workers and V2 D1 migrations are not ready. |
| Worker API | ready-local | `POST /api/reports` v3는 상태 신호와 같은 사용자·같은 장소 사진 ID, 정규화 해시태그, `clientRequestId`를 하나의 발행 묶음으로 저장한다. 타 사용자/타 장소 사진은 거부하고 동일 키 재시도는 기존 결과를 반환하며 pending 제보는 공개 집계와 realtime에서 제외한다. 승인 트랜잭션은 공개 상태·live signal·aggregate·publication·audit·deterministic outbox를 함께 기록하고 delivery는 승인 상태를 재검사한다. 운영 신고는 `/api/moderation/reports`로 분리한다. |
| D1 schema/seed | local-and-staging-through-0026, production-pending | Local SQLite applies through `0026_global_api_cost_guard.sql` and tests seed/migration idempotency, publication tables, storage/write/transform/read/release/session-issuance ledgers, hashed session/proof storage, private place-request ownership, per-session/global request guards, Workers/D1 60/70/80 reservations and reconciliation, concurrent review/delete fencing, ticket claims, HMAC per-IP upload/read daily ledgers, emergency controls, scheduler safety, cleanup/outbox delivery, and zero dead letters. Staging passes the same schema boundary, no-pending registry check, core seed evidence, and zero-state reconciliation. Production remains separate and unchanged. |
| R2/Images photo path | staging-r2-private, human-write-smoke-pending | The private staging bucket exists, `r2.dev` is disabled, no direct R2 custom domain is attached, incomplete multipart uploads abort after seven days, and temporary uploads expire after one day. R2/D1 reconciliation and admin resume passed; one bounded human Turnstile upload/moderate/read/delete smoke and return to `0 B` remain required before the path is operationally complete. |
| Durable Object realtime | ready-local | SQLite Durable Object namespaces, hibernating sockets that do not pin idle object duration, place/region/global fanout, preserved 101 WebSocket handle, pre-allocation valid-room gating, room cap 100, read-only client channel, 16 KiB event cap, room-scoped 50-event Durable Object persistence across reinitialization, 10-minute inactivity alarm deletion, bounded event validation, reconnect backoff, and 30-second polling fallback pass locally. Pages browser smoke also verifies place/region/global snapshot reads. |
| Admin/moderation guard | ready-local, blocked-operations-evidence | role gate, bulk hide/restore, delete, user restriction enforcement, coordinate-status enforcement, field-report pending/approve/reject/hidden queue, cache invalidation, and Next server Worker operation proxy tests pass. `docs/ugc-moderation-runbook.md` documents UGC ownership, SLA, intake queue, hide/restore/delete/restrict actions, evidence/audit, escalation, and stop conditions; `release:status` now guards required runbook sections and tokens |
| Cloudflare cost/usage guard | staging-generation-5-running, live-edge-evidence-pending | Photo guards retain the 3.2 GiB/12,800-write/4,000-Images-transform/800,000-Class-B/16,000-daily-read stops. The audited staging reconciliation advanced generation `5` to `running`; R2/D1 zero-state reconciliation also passed. The 2026-07-24 browser failover harness reverified zero API, NAVER provider, and analytics calls after directory mode. Live WAF and live proof that matching static assets avoid Worker invocation, plus alert delivery, remain external blockers because a request already reaching Worker code is counted. |
| TestFlight review notes guard | ready-local | `docs/testflight-review-notes.md` documents beta app description, reviewer instructions, permission copy, UGC moderation, privacy policy URL/support URL status, staging evidence requirements, and stop conditions; `release:status` guards required sections and tokens. Public HTTPS privacy/support URLs are now selected and live; named legal review and store metadata submission remain external. |
| Store privacy disclosure guard | ready-local, blocked-external-review | `docs/store-privacy-disclosure-draft.md` inventories precise/coarse location, photos, UGC, search, stable IDs, product interaction, and safe diagnostics; `release:status` fails if Apple/Google sections, native enforcement, console checklist, or stop conditions disappear. This is a local draft, not App Store Connect/Google Play Console submission or legal approval. |
| Real-device QA ledger guard | blocked-external | `docs/real-device-qa.md` now defines the required iPhone/Android QA matrix, staging environment fields, permission flows, UGC flows, redaction rules, crash checks, and artifact names; `release:status` guards the ledger structure. Actual iPhone/Android evidence remains blocked until staging URLs and R2 pass |
| Public privacy/support pages | ready-live | `src/app/privacy/page.tsx` and `src/app/support/page.tsx` provide the policy/support surfaces. On 2026-07-19 `https://silsigan-web-production.dudqks0319.workers.dev/privacy` and `/support` both returned HTTP 200 `text/html` and contained the app privacy, location, photo, report, deletion, and support terms. |
| Privacy/support URL guard | pass-selected | `.env.example` records distinct production HTTPS values for `SILSIGAN_PRIVACY_POLICY_URL` and `SILSIGAN_SUPPORT_URL`; `release:status` continues to reject placeholders, credentials, query params, fragments, localhost, or identical URLs. Store-console entry and named legal sign-off remain separate gates. |
| Region/area activation dashboard | ready-local | admin readiness dashboard tests and browser smoke pass |
| Legacy Supabase/Vercel artifacts | removed-local | `supabase/` migrations and `vercel.json` removed; runtime share URL defaults now use Cloudflare Pages, and `pnpm release:status` checks that Supabase project artifacts, Vercel deployment config, `@supabase/*` dependencies, and Vercel public runtime URLs are absent |

## Latest Local Verification

Historical verification rows below retain their original test counts for traceability; source commit `656eb315cbde4505b6c7db342a0185bb2762baea` is the 2026-07-22 post-security-remediation state with root `454/454` and mobile `4/4`. The remote branch tip will contain the later docs-only release record after the pending commit is pushed. Codex Security scan `7fe0ad74-161b-44e7-bbfb-fb0eeb07c6c2` remains the immutable vulnerable-range snapshot; its Medium finding is closed by the later source commit and fix report rather than rewritten in place.

The current browser source of truth is the 2026-07-20 390x844 run with 58 required checks, 614 redacted network events, `storesPostData=false`, `sensitiveHits=[]`, and artifact timestamp `1784510945721`; the browser required explicit report-place selection, directly clicked the per-photo shooting/publishing-rights confirmation before file input activation, uploaded and linked the photo through the unified publication contract, reopened the exact approved report deep link, rendered `#지금` while the legacy social feed was disabled, persisted its follow through the Worker preference API, returned from My, merged a cursor-based next page, explicitly selected a mocked NAVER result to prefill only name/address/category, proved that prefill sent no place-request POST, then submitted one exact-contract private place request and showed its owner-only `needs_verification` state in My. It also verified user-safe map fallback, required the exact `계정 삭제` phrase before permanent deletion, removed all mock-owned content including place requests, rotated to a new server-bound session, rejected the previous proof with 403, authenticated to the local admin surface, rendered aggregate-only plus Tier A/B fresh-coverage KPIs, exercised the five-meter R2/D1/Images stop-and-resume flow, and proved the three-meter global API guard stop -> fresh Cloudflare reconciliation -> below-70% resume flow. Evidence is under `artifacts/cloudflare-pages-smoke-worker-report-local`. This is local mock evidence; staging-live deletion and native real-device evidence remain open. Older dated rows are retained only as historical evidence.

The local store-listing candidate packet is now documented in `docs/store-listing-draft.md`. The 2026-07-20 `1784509404889` run produced reviewed home, place-detail, hashtag-search, and My screenshots from real app surfaces, with human Korean report copy, a photo-first place detail, and no inactive cross-device member promise. Its browser harness passed 58/58 required checks with 664 redacted network events, no request-body storage, and no sensitive hits. These are explicitly mock-data candidates, not App Store submission evidence. The map screenshot is excluded until NAVER owner-domain and origin enforcement evidence exists; all four candidates must be replaced with live-staging, real-approved-content, final-device captures before submission.

| Gate | Last result | Evidence |
| --- | --- | --- |
| V2 full local verification | pass, root 454 and mobile 4, 0 skipped | 2026-07-22 root `454/454`, mobile `4/4`, lint, typecheck, Next.js build, WebView check, OpenNext build, and staging/production web dry-runs pass. New coverage additionally proves visible-only single-leader non-overlapping background refresh, one `COST_GUARD_STATE` KV read per public request, and full visibility-cache reconciliation after security-sensitive mutations. Historical browser evidence remains 2026-07-20 local mock evidence and is not restated as live staging proof. |
| Public source fallback evidence | pass-local, 6/6 sources | `pnpm evidence:public-sources` runs the actual KMA, TourAPI, national parking, national traffic, national CCTV, and Seoul realtime adapters through success, fresh cache, simulated 503 outage, bounded stale/degraded fallback, and expired `insufficient`. The artifact reports `sensitiveHits=[]` at `artifacts/public-source-fallback-local/public-source-fallback.json`. This is deterministic fixture evidence, not rights or live-staging provider evidence. |
| NAVER Maps domain/operator preflight | staging-live-tiles-pass; owner-domain/recipient pending | The separate `Silsigan` Dynamic Map application and public Client ID remain configured without storing a Client Secret. Console hard limits remain daily `160,000`, monthly `4,800,000`, no over-limit use, and `70%` alerts. Staging web version `ad980039-5ba0-4ffb-9616-6f60eb6aea54` preserves the current map configuration; the prior verified `nrbe.pstatic.net` tiles and NAVER legal/logo evidence remains the current map proof. The shared host still does not satisfy the owner-domain gate, and notification-recipient evidence remains missing. |
| Capacitor WebView shell local/native smoke | pass-android-and-simulator; partial-ios-real-device | The actual staging URL/origin is synchronized into iOS/Android. On 2026-07-20 `pnpm --dir apps/webview check` and Android lint/debug APK build pass with Homebrew JDK 21; the APK SHA-256 remains `7439ae76464271f4dd26ae1ef7c4b5753b92ea52d49811b8d12244e53c4ae90f`, and no Android emulator/device is attached. The last clean Simulator screenshot remains the 2026-07-19 iPhone 17 evidence. On 2026-07-23 Xcode 26.3 recognized a physical iPhone 12 Pro / iOS 17.6.1, built an Apple Development-signed arm64 Debug app for `kr.silsigan.mobile` version `1.0 (1)`, validated the signature and exact staging config, installed it, launched it, and observed `WebView loaded` without a startup crash for 12 seconds. Screenshot-backed navigation, location allow/deny, camera/library, staging photo lifecycle, deletion, full-session crash, TestFlight release signing/archive privacy report, and push delivery remain open. |
| V2 dependency audit | pass-current | 2026-07-24 `pnpm audit:security` checks both root and mobile lockfiles at low severity and reports no known vulnerabilities after Next.js/`eslint-config-next` `16.2.11` and PostCSS `8.5.12` patches. |
| V2 390px browser smoke | pass-local | 2026-07-20 local Next + mock Worker smoke passes all 58 required checks: explicit report-place selection, per-photo rights confirmation and file-input activation, anonymous-session bootstrap, onboarding, home/map/my navigation, preference sync, explicit NAVER-result prefill with no pre-submit POST, private place-request create/owner status, user-safe map fallback, map controls, nationwide search, ranking/detail/marker, place/region/global realtime, like/comment/photo, place/comment/photo reports, owned-photo delete, JPEG upload, unified publication contract, report deep link, `SOCIAL_FEED_ENABLED=false` hashtag-to-latest-field-photo exploration, Worker-backed follow, My return, cursor pagination, cleanup, share/OG, exact-phrase account deletion and old-proof rejection, admin login, aggregate/Tier A/Tier B fresh-coverage KPIs, five photo-cost meters, three global API-cost meters, photo stop/resume, and global API stop/reconciliation/resume. The place-request evidence is `placeRequestCreationCount=1` with exact `clientRequestId/name/address/category` fields and owner-only `needs_verification`; deletion evidence is `accountDeletionCount=1`, `sessionIssueCount=2`, `sessionRotated=true`, and `remainingOwnedContentCount=0`. The latest network artifact has 614 events, no request body storage, and no sensitive hits; report target types `place`, `comment`, and `photo` are all present. Evidence: `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-home-1784510945721.png`, `pages-smoke-map-1784510945721.png`, `pages-smoke-place-1784510945721.png`, `pages-smoke-my-1784510945721.png`, `pages-smoke-hashtag-1784510945721.png`, `pages-smoke-account-deletion-1784510945721.png`, `pages-smoke-admin-cost-guard-stopped-1784510945721.png`, `pages-smoke-admin-cost-guard-running-1784510945721.png`, `pages-smoke-admin-api-cost-guard-stopped-1784510945721.png`, `pages-smoke-admin-api-cost-guard-running-1784510945721.png`, `pages-smoke-network-1784510945721.json`, and `pages-smoke-console-1784510945721.log`. This is local mock-data evidence, not staging-live or native real-device evidence. The separately refreshed store-candidate packet uses timestamp `1784509404889`. |
| R2 cost-guard admin Computer Use QA | pass-local-five-meters | 2026-07-19 Chrome authenticated through the same-origin JSON login route, rendered storage/write/Images-transform/monthly-read/daily-read as five distinct meters plus the 7-day aggregate KPI panel, stopped uploads and reads together with an audited reason, then required a new reason plus D1 ledger review acknowledgement before resume. The current Worker also independently rejects missing acknowledgement or any resume at/above a relevant 80% counter. Stopped/running evidence is `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-admin-cost-guard-stopped-1784459655424.png` and `pages-smoke-admin-cost-guard-running-1784459655424.png`. Staging `0020`-`0026`, the dedicated KV bindings, and Turnstile widget/config/secrets are present; live enforcement remains blocked by R2 checkout, post-`0022` R2/D1 reconciliation, configured API application deployment, and live smoke. |
| V2 Cloudflare external check | staging-resources-pass-api-degraded, production-blocked | R2 privacy checks, staging API/web histories, exact staging origin, D1 through `0026`, core seed evidence, no unsafe source targets, and dry-runs pass. The calibrated staging API is deployed but currently returns the intentional cost-guard `429` until audited reconciliation. Live Turnstile photo write, WAF/static-routing evidence, role-separated admin tokens, production secret/API, and production `D1_0006_NOT_APPLIED` remain blockers. |
| Visible local browser re-audit | pass-local, historical environment | In addition to the 43-check repository smoke, 2026-07-20 Computer Use opened the nationwide map, verified the `전국 검색`/`전국 최신 근거 TOP 10` surface and explicit sample labels, opened `?place=busan-gwangalli`, tested semantic place tabs with Arrow keys, confirmed global ArrowLeft no longer steals tab navigation, restored the originating scroll position, and verified that app back removes `place`/`report` query parameters. Computer Use also read the Cloudflare R2 checkout without accepting terms or changing account state. That run did not claim live staging API data; the later deployed staging API/web state in the active-truth section above supersedes its environment status without converting the local browser evidence into staging-live proof. |
| Local verify sequence | pass, 125 tests | 2026-06-25 local verification refresh after Worker posts/hashtags/questions D1 schema and UI routing, live Naver map/control clickability fix, fallback map status-banner hit-test fix, share/OG post Worker lookup guard, `/api/places` bbox/radius query coverage, Worker ranking KV read-through cache coverage, enriched ranking item payload coverage, D1 live-score ordering coverage, Durable Object WebSocket broadcast coverage, frontend scoped ranking client coverage, D1 hourly distinct anonymous user aggregation coverage, D1 field report source/status/TTL/coarse-radius/no-coordinate/list coverage, comment body privacy/script/URL-spam rejection coverage, comment daily/burst rate-limit coverage, comment/photo moderation report UI wiring, release-gate local Pages report baseline coverage, staging mutation admin-required fail-fast coverage, release-candidate final evidence bundling, production-candidate production read-only smoke wiring, collect-blockers release evidence coverage, release-gate failed-step summary coverage, Cloudflare external-state deployment URL and remote D1 `0002` migration coverage, D1/R2 release evidence operator planning, release-gate D1/R2 read-only evidence ordering, root release harness ledger/status/open-blocker, open-blocker evidence token, UGC moderation runbook section/token guard, Cloudflare cost/usage runbook section/token guard, TestFlight review notes section/token guard, real-device QA ledger guard, public privacy/support page guard, privacy/support URL shape guard, and Next Actions duplicate-line guard coverage, and release ledger refresh. 2026-06-27 `pnpm test -- tests/cloudflare-api.test.ts` passed 125 tests including D1 ranking abuse smoke, UGC moderation runbook guard coverage, Cloudflare cost/usage runbook guard coverage, TestFlight review notes guard coverage, real-device QA ledger guard coverage, public privacy/support page guard coverage, privacy/support URL guard coverage, auth-aware external-state blocker canonicalization, and Worker deployment absence classification. `pnpm cf:external-state` remains `blocked-external` on documented R2, missing Worker deployments, and deployment URL blockers |
| Targeted API tests | pass, 125 tests | 2026-06-27 local run keeps Supabase/Vercel legacy artifact and runtime public URL removal guards, root `release-ledger.yaml`/`RELEASE_STATUS.md` presence, required sections, `docs/current-release-state.md` source-of-truth linkage, open release blocker blocking, open blocker evidence token coverage in `RELEASE_STATUS.md`, duplicated Next Actions operator instruction blocking, UGC moderation runbook required-section/token blocking, Cloudflare cost/usage runbook required-section/token blocking, TestFlight review notes required-section/token blocking, real-device QA ledger required-section/token blocking, public privacy/support page token blocking, privacy/support URL required/HTTPS/no-localhost/no-query blocking, and secret-like value redaction guards, share/OG post Worker-first lookup, configured-Worker no-demo-fallback, no-Worker demo fallback coverage, Worker `/api/posts`, `/api/hashtags`, `/api/questions`, `/api/my-questions` memory fallback and SQLite D1 contract coverage, D1 posts/questions seed idempotency, release-order `0001_initial.sql` -> `0002_posts_questions.sql` migration-chain idempotency, Cloudflare external-state R2-disabled/bucket-missing/deployment-URL/D1-migration/auth-blocker/Worker-deployment classifiers and canonical blocker summary, D1 and R2 release evidence planner non-mutating defaults and production apply confirmation gates, release gate mutating admin-token preflight, staging mutation `--require-admin` fail-fast coverage, `--release-candidate` coverage for final mutating/report/photo/tail evidence, `--production-candidate` coverage for HTTPS production Pages/API URLs and production API/Pages read-only smoke without write smoke, collect-blockers non-mutating guard coverage, failed collect-blockers summary coverage, `/api/places` radius parsing/D1 filtering/client-coordinate non-echo coverage, `/api/rankings` `CACHE` KV miss/hit/TTL/corrupt-entry recovery and enriched item field coverage, D1 ranking abuse smoke for repeated same-user click/like signals, Worker Durable Object WebSocket broadcast coverage, frontend scoped ranking client query coverage for region/area/category/bbox TOP 10 calls, D1 live-surface coverage for same anonymous user click/comment/photo contributing one hourly `unique_user_count` and live-score ordering, D1 `/api/reports` field-report coverage for source `field_report`, coarse verified radius, 3-hour TTL, public listing, `LOCATION_NOT_VERIFIED`, and coordinate/photo URL non-echo, memory fallback field report listing without raw location data, comment body rejection coverage for phone, resident-id, script, and URL-spam patterns without echoing raw body, local Pages report baseline ordering and network artifact redaction guard including `onboarding.dismiss`, coordinate-status preflight, Worker admin token precedence, release gate, Pages smoke hardening, MVP ranking max-limit alignment, comment burst/daily rate limits, and Worker moderation report client coverage under `pnpm test -- tests/domain.test.ts` |
| Browser map/control regression smoke | pass | 2026-06-25 local Pages report smoke against local Next verified the live/fallback map area is visible, initial map tools and current-location button are not covered by bottom nav, bottom nav is opaque so hidden ranking buttons do not look tappable underneath it, `필터`/`교통 켜기`/`이 지역 다시 검색`/header buttons/bottom nav hit-test to their own controls, first-visit onboarding dismisses by real click, traffic toggles state, requery shows toast feedback, the fallback map status banner does not intercept marker hit-tests, and `광안리해수욕장` ranking item plus map marker both open place detail. Screenshots/artifacts: `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-1782366595310.png`, `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-network-1782366595311.json`, `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-console-1782366595312.log` |
| Browser map/detail smoke | pass | Playwright/in-app browser checked 390x844 mobile: Naver auth 401 renders clickable fallback map instead of a blank SDK frame, marker opens detail, filter/traffic/requery/region/bottom nav buttons update state, Next dev indicator no longer covers bottom nav, Seoul empty-region map keeps `표시할 장소 없음` frame, screenshots saved under `/tmp/silsigan-map-clickability-qa-2026-06-19` and Playwright viewport artifacts |
| Browser home-button smoke | pass | Chrome headless CDP against local Next dev: initial `지도` view has a non-empty fallback map surface, `홈` navigation works, top notification button toggles `aria-pressed=true` and toast text, `주차`/`야경` feed tabs update counts and empty state; screenshots `/tmp/silsigan-map-smoke-2026-06-19.png`, `/tmp/silsigan-home-button-smoke-2026-06-19.png` |
| Browser map-bounds fetch smoke | pass | Chrome headless CDP against local Next dev captured Network requests: initial `/api/places?limit=100`, then bounds emission triggered `/api/places?bbox=129.118600%2C35.153200%2C129.311500%2C35.838200&limit=100`; screenshot `/tmp/silsigan-map-bounds-fetch-smoke-2026-06-19.png` |
| Browser place-click Worker smoke | pass | Chrome headless CDP with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4319`: fallback map marker click opened `광안리해수욕장` detail and POSTed `http://127.0.0.1:4319/api/places/busan-gwangalli/click`; screenshot `/tmp/silsigan-place-click-worker-smoke-2026-06-19.png` |
| Browser comment-create Worker smoke | pass | Chrome headless CDP with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4320`: ranking click opened `광안리해수욕장`, comment composer POSTed `http://127.0.0.1:4320/api/comments`, rendered the new comment and `댓글 업데이트` realtime strip; screenshot `/tmp/silsigan-comment-create-worker-smoke-2026-06-19.png` |
| Browser like/report Worker smoke | pass | Playwright with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4321`: Naver auth 401 switched to clickable fallback map without app crash, `광안리해수욕장 상세 열기` opened detail, `좋아요` POSTed `/api/places/busan-gwangalli/like` and showed `좋아요 반영`, `장소 신고` POSTed `/api/moderation/reports` and showed `신고 접수`; artifacts `artifacts/silsigan-like-report-worker-smoke-2026-06-19.png`, `artifacts/silsigan-like-report-worker-network-2026-06-19.json`, `artifacts/silsigan-like-report-console-current-2026-06-19.log` |
| Browser photo-click Worker smoke | pass | Playwright with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4322`: `광안리해수욕장` detail loaded Worker photo via `/api/photos?placeId=busan-gwangalli&limit=12`, clicking `1KB 현장 사진` POSTed `/api/photos/photo_click_smoke_1/click`, updated tile from `클릭 4` to `클릭 5`, and showed `사진 확인이 반영됐습니다.`; artifacts `artifacts/silsigan-photo-click-worker-smoke-2026-06-19.png`, `artifacts/silsigan-photo-click-worker-network-2026-06-19.json`, `artifacts/silsigan-photo-click-console-current-2026-06-19.log` |
| Browser photo-preview Worker smoke | pass | Playwright with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4323`: Worker photo list returned `previewUrl`, browser requested `GET /api/photos/photo_preview_smoke_1/file` for the tile background, tile click sent POST `/api/photos/photo_preview_smoke_1/click`, and count went `2 -> 3`; artifacts `artifacts/silsigan-photo-preview-worker-smoke-2026-06-19.png`, `artifacts/silsigan-photo-preview-worker-network-2026-06-19.json`, `artifacts/silsigan-photo-preview-console-current-2026-06-19.log` |
| Browser my-menu smoke | pass | Chrome headless CDP against local Next dev: `마이` tab visible, `내 제보`, `저장한 게시물`, `차단한 사용자` menu buttons set `aria-pressed=true`, scroll to `내 최근 제보`/`저장한 게시물`/`안전 보호`, screenshot `/tmp/silsigan-my-menu-runtime-2026-06-19.png` |
| Browser admin-filter smoke | pass | Chrome headless CDP against `/admin/moderation/posts`: `차량번호`, `임시 숨김`, `전체` filter buttons set `aria-pressed=true` and update Worker/demo counts and empty/list states, screenshot `/tmp/silsigan-admin-filter-smoke-2026-06-19.png` |
| Browser admin-action smoke | pass | Chrome headless CDP against `/admin/moderation/posts`: demo queue `유지` action posts to admin route, clears the queued item, updates count to `데모 게시물 0건`, and shows completion notice, screenshot `/tmp/silsigan-admin-action-smoke-2026-06-19.png` |
| Admin post moderation input smoke | pass | Local Next dev `POST /api/admin/moderation/posts` returned 400 `VALIDATION_ERROR` for missing `postId` and invalid `action=publish`, while a valid queue `keep` action returned 200 |
| Browser realtime smoke | pass | Chrome headless CDP against mock Worker API: detail shows `Cloudflare DO`/`댓글 업데이트`, POST like and POST moderation report reached API, screenshot `artifacts/realtime-event-visible-smoke-2026-06-19.png` |
| Browser comment/photo report smoke | pass | 2026-06-20 in-app Browser against local Next + mock Worker API: Naver marker rendered, marker click opened detail, Worker photo report opened reason modal and POSTed `/api/moderation/reports` with `targetType=photo`, new Worker comment rendered a comment report button, comment report reason modal POSTed `/api/moderation/reports` with `targetType=comment`, toast and realtime `신고 접수` state were visible. Browser CDP screenshot timed out twice, so evidence is live DOM, console logs, and mock Worker moderation report receipts |
| `pnpm release:status` | blocked-external | 2026-07-20 release status loads only the six allowlisted public release URL keys from `.env.example` when the corresponding process variables are absent; explicit process values, including empty strings, still win and fail closed. Privacy, support, staging Pages, and production Pages URLs now pass without false blockers. The remaining direct status blockers are `release_harness.ledger.open_blockers`, `deployment_url.staging.worker_api`, and `deployment_url.production.worker_api`; R2, staging API deployment and live Turnstile smoke, live WAF/static routing, NAVER owner-domain registration, live operations, real-device QA, legal evidence, and production D1 remain tracked in the ledger. |
| `pnpm cf:typegen` | pass | 2026-06-20 `wrangler types cloudflare-env.d.ts --env-interface CloudflareEnv --env-file .env.example --include-runtime false` generated Cloudflare env types from tracked sample keys, not local personal `.env.local` keys; generated type files are gitignored |
| `pnpm cf:build` | pass | 2026-07-24 OpenNext Cloudflare build generated `.open-next/worker.js` and `.open-next/assets` for Next.js 16.2.11 with `@opennextjs/cloudflare` 1.19.11 |
| `pnpm cf:web:dry-run*` | pass | 2026-06-20 root frontend `wrangler.jsonc` development/staging/production dry-runs read 89 assets from `.open-next/assets` and validated the `ASSETS` binding without deploying |
| `pnpm cf:external-state` | blocked-external | Its D1 classifier uses a schema-only boundary before any data query. The latest 2026-07-20 Staging check passes through `0026`, reports no pending migration, and verifies registry plus core seed evidence. Production returns `D1_0006_NOT_APPLIED`. Staging Turnstile provisioning is complete at the widget/config/secret-storage layer, but R2, configured staging API application deployment/API URL, live validation, production Turnstile/API state, and production D1 remain external blockers; dedicated cost-guard KV bindings pass account/config inspection. |
| `pnpm cf:d1:evidence` | staging pass through 0026; production pending | The guarded recovery used a protected external backup, exact legacy-shape preflight, local restore drill, normal Wrangler migration apply, and independent postflight. The latest read-only classifier verifies the Staging boundary through `0026`, no pending migrations, aligned registry, and core seed evidence. Production remains separately approval-gated. |
| `pnpm cf:r2:evidence` | pass-staging-private | Staging evidence confirms the configured private bucket, disabled `r2.dev`, absent direct R2 custom domain, and lifecycle rules. Production bucket evidence remains separate and was not changed. |
| `pnpm audit:security` | pass | 2026-07-24 root and mobile low-severity audits both report no known vulnerabilities after patching Next.js/`eslint-config-next` to `16.2.11` and PostCSS to `8.5.12`. |
| `pnpm cf:tail:capture` + `pnpm smoke:tail-redaction` | pass staging-safe | On 2026-07-24 the bounded exact-staging-Worker capture processed 20 Wrangler events and 88,840 raw bytes in memory, removed platform request/response metadata and request paths, persisted only an owner-readable 10,688-byte sanitized artifact, and passed with `sensitiveFindings=[]`. The validator still rejects secrets, anonymous proofs/IDs, exact coordinates, emails, filenames, and R2 object keys emitted by Worker logs. Evidence: `artifacts/cloudflare-tail/staging-tail-safe-v2-20260724.log`. |
| `pnpm smoke:staging` without API URL | fail expected | `SILSIGAN_STAGING_API_BASE_URL=` and `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=` returns JSON `ok:false` with `BASE_URL_REQUIRED`; `--tail-only --tail-file=tests/fixtures/redacted-worker-tail.log` still passes without a base URL |
| `pnpm smoke:pages` | pass local mock | Historical local browser runs remain under `artifacts/cloudflare-pages-smoke-*`. The current 2026-07-20 `pnpm smoke:pages:local-report` source of truth passes 58 required checks, including explicit report-place selection, per-photo rights confirmation and file-input activation, preference sync, explicit NAVER-result prefill with no pre-submit POST, private place-request create/owner status, user-safe map fallback with non-visible diagnostics, nationwide map/search, JPEG upload/delete, unified field-report publication and deep link, `SOCIAL_FEED_ENABLED=false` hashtag-to-approved-photo exploration, Worker-backed follow, My return, cursor pagination, moderation target types, cleanup, share page/OG image, local-only account deletion with zero remaining owned content and anonymous-session rotation/old-proof 403 rejection, admin login, aggregate/Tier A/Tier B fresh-coverage KPIs, five photo-cost meters, three global API-cost meters, photo stop/resume, and global API stop/fresh reconciliation/below-70% resume. Latest redaction summary is `eventCount=614`, `reportTargetTypes=comment/photo/place`, `storesPostData=false`, `sensitiveHits=[]`, `passedRequiredChecks=58`, `failedChecks=[]`; artifacts use timestamp `1784510945721`. Earlier baselines are retained only for traceability. |
| `pnpm release:gate -- --plan-only` | pass local plan | Plan-only lists local verify, optional local Pages report baseline, optional Workers tail redaction, strict `release:status`, critical audit, `cf:typegen`, `cf:build`, frontend development/staging/production dry-runs, `cf:preflight`, staging/production `cf:r2:evidence -- --check`, staging/production `cf:d1:evidence -- --check`, `cf:external-state`, API staging/production Wrangler dry-runs, staging Worker smoke, Pages browser smoke, and optional coordinate-status smoke preflight without printing secret values. `--collect-blockers` recursively extracts the current production `D1_0006_NOT_APPLIED` code while normalizing duplicate URL/R2 aliases; Staging D1 no longer contributes a migration blocker. |
| `pnpm release:gate -- --skip-verify --skip-dry-run --collect-blockers --tail-file=tests/fixtures/redacted-worker-tail.log --timeout-ms=120000` | blocked-external expected | 2026-06-26 non-mutating blocker collection returned `resultCounts.pass=9` and `resultCounts.fail=7`. Passing steps were tail redaction fixture, critical audit, Cloudflare typegen, OpenNext build, frontend development/staging/production dry-runs, staging D1 evidence, and production D1 evidence with `posts=4`, `questions=3`. Failing steps are strict release status, URL/resource preflight, staging/production R2 evidence, external-state, staging API smoke, and Pages smoke; the top-level blocker summary is normalized to `release_harness.ledger.open_blockers`, missing staging/production Pages/API deployment URLs, and `R2_NOT_ENABLED` |
| `pnpm cf:preflight` | fail by design | D1/KV/resource bindings pass; staging/production Pages/API deployment URLs are not configured |
| `wrangler deploy --env staging` | blocked-external | Cloudflare API returned R2 enablement required for `silsigan-photos-staging` |

## Cloudflare External State

| Item | Current state | Required evidence before release |
| --- | --- | --- |
| Cloudflare web staging deployment | live-api-pass-kv-remediated-turnstile-photo-smoke-pending | OpenNext staging version `ad980039-5ba0-4ffb-9616-6f60eb6aea54` is deployed with the exact staging API base and visible single-leader 60-second lightweight refresh. API version `dc07bf4a-879a-421c-aea1-5418f9c8bc0e` performs one cost-guard KV read per public request. The browser returned to live mode; its smoke tab was then closed to avoid unattended polling. A real bounded JPEG reached the managed Turnstile challenge historically, but human verification and upload/moderate/read/delete remain pending. |
| Staging web rollback drill | read-only-candidate-ready, mutation-not-run | `artifacts/cloudflare-rollback-drill/staging-web-readonly.json` records current version `e87e79d5-5d30-43dc-adfe-1b5393a3b8d2` and previous stable version `d7c2bab9-101f-4d2c-a8c3-0b02e9f6e14e`, with `mutationsPerformed=false` and no author/message/raw-error output. An actual interactive rollback, post-rollback smoke, and restoration still require explicit approval after API-backed staging is healthy. |
| Cloudflare API staging deployment | deployed-running-kv-remediated | Version `dc07bf4a-879a-421c-aea1-5418f9c8bc0e` is live. Role-separated admin credentials and denial boundaries remain verified. The rotated Turnstile secret is installed by name only, public requests reuse the preloaded KV cost-control row, and generation `5` remains the last audited running-state evidence. |
| Cloudflare web production deployment | deployed-history, promotion-blocked | 2026-07-19 dashboard identifies `https://silsigan-web-production.dudqks0319.workers.dev`; production promotion and environment wiring remain prohibited until staging evidence is clean and separately approved. |
| Cloudflare API production deployment | missing | Deploy configured API Worker `silsigan-api-production`, set `SILSIGAN_PRODUCTION_API_BASE_URL=https://...`, and run `pnpm release:gate -- --production-candidate` API read-only smoke |
| Staging D1 database ID | pass-through-0026 | Guarded recovery and normal Wrangler migrations completed. The latest 2026-07-20 read-only check passes the schema boundary through `0026`, reports no pending migration, and verifies registry plus core seed evidence. The dedicated KV is bound and a protected pre-change external backup remains preserved. |
| Staging KV namespace ID | ready-remote | `workers/api/wrangler.jsonc` has concrete `env.staging.kv_namespaces[CACHE].id` |
| Production D1 database ID | remote-visible, `D1_0006_NOT_APPLIED`; V2 through `0026` unproven | Production apply requires separate approval after R2/API deployment, staging mutation/read verification, rollback evidence, and clean staging gates. |
| Production KV namespace ID | ready-remote | `workers/api/wrangler.jsonc` has concrete `env.production.kv_namespaces[CACHE].id` |
| R2 account enablement | staging-active-private | The private staging bucket and lifecycle/privacy evidence pass. Production R2 setup remains separate and requires its own approval. Photo writes remain stopped until reconciliation and bounded smoke rather than being enabled merely because the subscription exists. |
| Turnstile upload protection | staging-secret-rotated-managed-exact-host-human-check-pending | The previously exposed staging Turnstile secret was retired and a replacement was installed directly into the staging Worker secret store without writing or printing its value. Cloudflare can retain the retired value for up to a two-hour rotation grace period. The managed widget includes the exact staging and production web hosts; `/api/config` exposes only public configuration and no secret key. Human verification, full upload lifecycle evidence, and the production secret remain pending. |
| R2/Cloudflare Images/admin staging mutation | not run | Set `SILSIGAN_STAGING_ADMIN_TOKEN`, then run `SILSIGAN_STAGING_MUTATION=1 pnpm smoke:staging -- --require-admin`; coordinate-status smoke additionally requires explicit `SILSIGAN_STAGING_COORDINATE_SMOKE_PLACE_ID`, latitude, and longitude |
| Workers platform tail redaction | pass-staging-safe | `pnpm cf:tail:capture -- --output=artifacts/cloudflare-tail/staging-tail-safe-v2-20260724.log --duration-ms=20000` captured 20 events without persisting raw Wrangler output; `pnpm smoke:tail-redaction -- --tail-file=artifacts/cloudflare-tail/staging-tail-safe-v2-20260724.log` passed with no sensitive findings. |

## Release Decision

Current state: `blocked-external`

Do not claim production readiness until all of the following are true:

1. `pnpm release:status -- --strict` passes.
2. `pnpm cf:preflight` passes with real staging/production URLs.
3. `pnpm smoke:staging` passes against the staging Worker URL.
4. `SILSIGAN_STAGING_MUTATION=1 pnpm smoke:staging -- --require-admin` passes against staging R2/Images, admin moderation, and user restriction with `SILSIGAN_STAGING_ADMIN_TOKEN` set in the shell.
5. `pnpm smoke:tail-redaction` continues to pass against a real artifact produced only by `pnpm cf:tail:capture`; raw Wrangler tail output must never be redirected to disk.
6. `pnpm smoke:pages` confirms Cloudflare Pages frontend can drive visible map controls, map/detail, Worker read/telemetry paths, and, with explicit mutation flags, comment/photo/report flows through the staging Worker API.
7. `docs/real-device-qa.md` is filled with iPhone and Android device evidence for launch, map, location allow/deny, camera/photo library, comment/photo/like/ranking/report/moderation, redaction, and crash checks.

## Local Phase 1 closeout (2026-07-14)

- [x] Server feature gates, client fetch gates, credit-ledger fail-closed behavior, and `expiresAt` enforcement.
- [x] V2 aggregate status is the only truth-bearing decision path; feed/share/OG no longer promote one post's values to a visit decision.
- [x] Field-report moderation is locally end-to-end: pending submit, private owner history, moderator queue, approve/reject action, approval-only atomic public signal/aggregate/publication/outbox, delivery-time approval recheck, vote gate, status recomputation, and audit action.
- [x] Required analytics event names are wired to the client and Worker allowlist with redaction tests.
- [x] Local full verification: current test set 428/428 with 0 skipped, `pnpm lint`, `pnpm typecheck`, `pnpm build`, `pnpm cf:build`, staging web dry-run, `git diff --check`, `pnpm --dir apps/webview check`, mobile verify, and Android JDK 21 lint/debug APK pass.
- [x] Cloudflare local packaging: `pnpm cf:typegen` and `pnpm cf:build` pass; OpenNext generates `.open-next/worker.js` and assets. No Cloudflare deployment or migration was performed.
- [x] Capacitor project generation and privacy metadata: iOS/Android skeletons exist locally for `kr.silsigan.mobile`; actual staging URL injection, Android debug lint/build, iOS Simulator evidence, and a 2026-07-23 Apple Development-signed iPhone 12 Pro build/install/launch/startup-console smoke pass. TestFlight release signing, archive privacy report, screenshot-backed full real-device flows, and Android device results are not claimed.
- [x] Repository local browser smoke passes at 390x844: 58 required interactions, including per-photo rights confirmation and file-input activation, hashtag Worker follow, My return, cursor pagination, explicit NAVER-result prefill with no pre-submit POST, private place-request create/owner status, server-bound anonymous-session bootstrap, exact-phrase account deletion, zero remaining mock-owned content, new-session rotation, old-proof 403 rejection, aggregate/Tier A/Tier B fresh-coverage KPIs, photo and global API guard stopped/running screenshots, 614 redacted network events, `storesPostData=false`, and `sensitiveHits=[]`. Latest security artifacts use timestamp `1784510945721`; the refreshed store-candidate artifacts use timestamp `1784509404889` and independently pass the same 58 required checks with 664 redacted events, no request-body storage, and no sensitive hits. The local mock Worker is intentionally not staging-live or native real-device evidence.
- [x] Cloudflare staging has the private R2 bucket, lifecycle/privacy controls, deployed API/web URLs, exact browser origin, Turnstile/HMAC secret names, D1 through `0026`, and historical read-only API/browser evidence. The audited path resumed cost guard generation `5` to `running`; a human Turnstile photo lifecycle and zero-residual proof remain open. Computer Use also created the NAVER `Silsigan` Dynamic Map application, configured only its public Client ID, and saved daily/monthly hard limits plus 70% alerts; no Client Secret was printed or stored. Cloudflare still has no owner domain and NAVER has no notification recipient, so those production gates remain external.

## Next Actions (external release gates)

1. Complete user login/CAPTCHA for data.go.kr, then prepare KMA and national-parking utilization applications without submitting them.
2. Prepare TourAPI OAuth/application and retain the ITS traffic/CCTV-metadata application at its final review step.
3. Obtain action-time confirmation before provider submissions, OAuth grants, or any future staging admin-token rotation. Role-separated `ADMIN_TOKENS` and the three existing provider keys are already installed in staging only; keep every official source and ingestion target disabled.
4. Validate rights, attribution, quota, TTL, region, health, fixture, and failure fallback one source at a time; activate KMA first only after its source-policy audit passes.
5. Reconcile the empty staging R2 bucket against D1, create role-separated admin tokens, and resume photo writes only through the audited admin path with all counters below 70%.
6. Capture one bounded upload/moderate/read/delete smoke, Turnstile success/failure, cleanup, zero residual R2 bytes, admin authorization failures, and 60/70/80 guard evidence. Workers tail redaction is already captured safely and must be repeated after the mutation smoke.
7. Add WAF/rate-limit and alert-recipient evidence, then choose an owner-controlled domain and complete the NAVER production-origin packet. Shared `workers.dev` remains staging-only evidence.
8. Finish screenshot-backed location, camera/library, photo lifecycle, mutation/deletion, and full-session crash QA on the already installed iPhone staging build; then configure/install the Android real-device build and complete TestFlight release archive signing/privacy evidence in `docs/real-device-qa.md`.
9. Record named moderation/on-call owners and complete legal review for privacy, location, UGC, deletion, public-source terms, store labels, and final public URLs. Ads remain disabled.
10. Apply production migrations or deploy/promote production only after clean staging evidence and separate production approval, then rerun strict release status and production-candidate read-only smoke.
