# #실시간 current release state

Updated: 2026-07-10
Branch: `codex/silsigan-progress-20260710`
Local base commit: `8198cf745d59895ae28bdd46bc8b32016505308a` plus uncommitted V2 implementation

## Objective

기존 Supabase 기반 지역 MVP를 Cloudflare Workers/D1/R2/Durable Objects 기반 전국 실시간 장소 앱으로 전환한다. 완료 판정은 로컬 테스트 통과가 아니라 Cloudflare staging/production 리소스와 운영 증적까지 분리 검증된 상태를 기준으로 한다.

Current release target: App Store production submission is not the next milestone. The current milestone is a Cloudflare-backed TestFlight MVP with live staging URLs, R2/Images evidence, staging smoke, and real-device QA. See `docs/testflight-readiness.md`.

Next unblocked operator packet: `docs/cloudflare-staging-operator-packet.md`.

V2 completion contract: [v2-decision-register.md](v2-decision-register.md). The 11 product, platform, privacy, source-rights, and mobile decisions are fixed there. Local implementation is complete for decisions 1-10 and the hybrid identity seam for decision 11; external console, rights, migration, deployment, legal, and real-device evidence remains separate and is not represented as complete.

## Local Code State

| Area | State | Evidence |
| --- | --- | --- |
| V2 visit-decision UX | ready-local | Home now prioritizes `지금 방문해도 되는가`, shows source/observed time/confidence/expiry, keeps five-place list fallback beside map recovery, starts field reports with no preselected observation, and allows a general report when location is denied. Q&A, rewards, ads, live streams, Seoul realtime, social feed, and demo data default off. |
| V2 decision engine and identity | ready-local | Shared contracts enforce dimension TTLs, expiry, conflict handling, independent-source consensus, `insufficient` for missing information, anonymous/member namespace separation, signed member-link seam, blocks, report votes, and account deletion. Anonymous remains default; external member authentication is not enabled. |
| V2 source registry and adapters | ready-local, activation-blocked | KMA, TourAPI, national parking standard, ITS traffic, and ITS CCTV adapters validate provider shapes through a redacted retry/cache/circuit gateway. Static sources cannot become current signals; CCTV stream URLs are dropped and playback remains disabled. Admin source activation requires rights review, attribution, region, TTL, owner, and healthy/degraded state. |
| V2 D1 migrations | ready-local, blocked-remote | Additive migrations `0004_v2_foundation.sql`, `0005_v2_signals.sql`, and `0006_trust_safety_identity.sql` pass local SQLite chain/idempotency tests. The 2026-07-10 read-only external check reports `D1_0006_NOT_APPLIED` for both staging and production; no remote migration was applied in this work. |
| Capacitor WebView shell | ready-local, blocked-native-evidence | `apps/webview`, app id `kr.silsigan.mobile`, exact HTTPS origin navigation, system-browser external links, schema-validated bridge, coarse-only location response, camera/library/share/network/keyboard/back/deep-link seams, and deployed Next bootstrap are implemented and tested. Native projects, signing, custom settings adapter, staging build, and real-device evidence remain external work. |
| Photo privacy and moderation | ready-local, blocked-r2-evidence | Original files are not retained; metadata is removed, pixels are re-encoded, original filenames are excluded, exact duplicates are rejected, and images remain pending until moderation approval. Live R2/Images evidence is blocked by `R2_NOT_ENABLED`. |
| Next.js frontend | ready-local | 지도 SDK 인증/타일 실패 시 fallback 유지, SDK ready 후 빈 지도 방지, 모바일 지도 컨트롤 클릭 영역 보정, 전국 실시간 베타 헤더 카피, Worker-backed 장소/게시물/해시태그/질문 read path, share/OG post lookup Worker-first path, 선택 지역 scoped fetch, 전국/지역/지도 화면 안 TOP 10 랭킹 패널과 랭킹 항목 상세 열기 smoke, Worker 사진 place-scoped fetch/preview/click 동기화, 상세 시트, 홈 피드 탭 필터, 팔로우 기반 알림 토글, Worker 좋아요/장소·댓글·사진 신고 동기화, realtime room polling UI smoke 증적은 `docs/security-gate.md`에 기록. OpenNext Cloudflare adapter config와 pinned dependency가 있으며 `pnpm cf:typegen`은 `.env.example` 기반으로 생성되고 `pnpm cf:build`가 `.open-next/worker.js`와 `.open-next/assets`를 생성한다 |
| Worker API | ready-local | `workers/api/src/index.ts`, `workers/api/wrangler.jsonc`; `/api/posts`, `/api/hashtags`, `/api/questions`, `/api/my-questions`가 D1/메모리 fallback으로 공개 feed 계약을 제공한다. `POST /api/reports` 현장 제보는 D1 `place_events.source = field_report`로 상태/검증 반경/TTL만 저장하고, `GET /api/reports`는 field report 공개 목록만 반환하며, 운영 신고는 `/api/moderation/reports`로 분리. D1 ranking abuse smoke는 같은 익명 사용자의 반복 장소 클릭/좋아요가 랭킹 카운트와 live score를 부풀리지 않음을 검증한다 |
| D1 schema/seed | ready-local, staging/production-applied | `0002_posts_questions.sql`로 posts/questions D1 테이블과 인덱스가 추가됐고 local SQLite는 `0001_initial.sql` -> `0002_posts_questions.sql` migration chain 재적용과 seed 재적용 후 6 places, 6 rankings, 4 posts, 3 questions idempotency를 통과했다. 2026-06-25 `pnpm cf:d1:evidence -- --env=staging --apply --timeout-ms=120000`로 staging D1에 `0002` migration과 seed를 원격 적용했고, 후속 `--check`는 pending migration 없음과 `posts=4`, `questions=3`을 확인했다. 2026-06-26 `pnpm cf:d1:evidence -- --env=production --apply --confirm-production --timeout-ms=120000`로 production D1에도 `0002_posts_questions.sql`과 seed를 적용했고, 후속 production `--check`는 no pending migrations, `posts=4`, `questions=3`을 확인했다 |
| R2/Images photo path | ready-local, blocked-dashboard | fake binding tests, Worker read proxy, browser upload/preview smoke recorded in `docs/security-gate.md`; `pnpm cf:r2:evidence -- --env=staging`는 non-mutating R2 operator plan을 출력한다. 2026-06-26 `pnpm cf:r2:evidence -- --env=staging --check --timeout-ms=120000` still returns `R2_NOT_ENABLED`. Cloudflare R2 docs require adding an R2 subscription through Dashboard checkout before bucket evidence can pass: https://developers.cloudflare.com/r2/get-started/ |
| Durable Object realtime | ready-local | Worker route alias, place/region/global fanout tests, WebSocket broadcast test, frontend place room polling path, and Pages browser smoke for place/region/global realtime room fetches pass locally |
| Admin/moderation guard | ready-local | role gate, bulk hide/restore, delete, user restriction enforcement, coordinate-status enforcement, cache invalidation, Next server Worker operation proxy tests pass. `docs/ugc-moderation-runbook.md` documents UGC ownership, SLA, intake queue, hide/restore/delete/restrict actions, evidence/audit, escalation, and stop conditions; `release:status` now guards required runbook sections and tokens |
| Cloudflare cost/usage guard | ready-local | `docs/cloudflare-cost-usage-runbook.md` documents Usage & billing dashboard checks, Billing alerts, R2/D1/Workers/Durable Objects/Cloudflare Images metrics, daily/weekly evidence cadence, budget thresholds, and TestFlight stop conditions; `release:status` guards required sections and tokens |
| TestFlight review notes guard | ready-local | `docs/testflight-review-notes.md` documents beta app description, reviewer instructions, permission copy, UGC moderation, privacy policy URL/support URL status, staging evidence requirements, and stop conditions; `release:status` guards required sections and tokens. Final HTTPS privacy/support URLs remain an external readiness blocker before external TestFlight submission |
| Real-device QA ledger guard | blocked-external | `docs/real-device-qa.md` now defines the required iPhone/Android QA matrix, staging environment fields, permission flows, UGC flows, redaction rules, crash checks, and artifact names; `release:status` guards the ledger structure. Actual iPhone/Android evidence remains blocked until staging URLs and R2 pass |
| Public privacy/support pages | ready-local, blocked-url | `src/app/privacy/page.tsx` and `src/app/support/page.tsx` provide local public policy/support surfaces for TestFlight review, and `release:status` guards required data-handling, support, report, deletion, and final URL tokens. Final live HTTPS URLs still require deployed Pages/Worker URLs and `SILSIGAN_PRIVACY_POLICY_URL` / `SILSIGAN_SUPPORT_URL` |
| Privacy/support URL guard | blocked-external | `release:status` now requires `SILSIGAN_PRIVACY_POLICY_URL` and `SILSIGAN_SUPPORT_URL` to be distinct HTTPS URLs without placeholders, credentials, query params, fragments, or localhost hosts before external TestFlight review notes can be submitted |
| Region/area activation dashboard | ready-local | admin readiness dashboard tests and browser smoke pass |
| Legacy Supabase/Vercel artifacts | removed-local | `supabase/` migrations and `vercel.json` removed; runtime share URL defaults now use Cloudflare Pages, and `pnpm release:status` checks that Supabase project artifacts, Vercel deployment config, `@supabase/*` dependencies, and Vercel public runtime URLs are absent |

## Latest Local Verification

| Gate | Last result | Evidence |
| --- | --- | --- |
| V2 full local verification | pass, 184 tests | 2026-07-10 `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` pass. Full tests are `184/184`; the production Next.js 16.2.6 build compiles without warnings and generates all 21 routes. Coverage includes V2 decision/TTL/source consensus, public adapters, D1 migration chain, source activation policy, trust-safety identity, photo privacy, Capacitor bridge/navigation, release gates, and negative auth/validation paths. |
| V2 dependency audit | pass | 2026-07-10 `pnpm audit --audit-level critical` reports no known vulnerabilities after the Capacitor runtime dependencies were added. |
| V2 390px browser smoke | pass | 2026-07-10 local Next + mock Worker smoke passes all 31 required checks: onboarding, home/map/my navigation, map fallback and controls, search, ranking/detail/marker, place/region/global realtime, like/comment/photo, place/comment/photo reports, selected field report, cleanup, share page, and OG image. Network artifact has 206 events, no request body storage, and no sensitive hits. Evidence: `artifacts/silsigan-v2-local-report-20260710/pages-smoke-home-1783658142487.png`, `pages-smoke-map-1783658142487.png`, `pages-smoke-network-1783658142487.json`, and `pages-smoke-console-1783658142487.log`. |
| V2 Cloudflare read-only external check | blocked-external | 2026-07-10 `pnpm cf:external-state`: auth and staging/production dry-runs pass; staging/production web Workers have deployment history. Blockers are `R2_NOT_ENABLED`, missing staging/production API Worker deployments, four missing deployment URLs, and `D1_0006_NOT_APPLIED` for staging and production. |
| Local verify sequence | pass, 125 tests | 2026-06-25 local verification refresh after Worker posts/hashtags/questions D1 schema and UI routing, live Naver map/control clickability fix, fallback map status-banner hit-test fix, share/OG post Worker lookup guard, `/api/places` bbox/radius query coverage, Worker ranking KV read-through cache coverage, enriched ranking item payload coverage, D1 live-score ordering coverage, Durable Object WebSocket broadcast coverage, frontend scoped ranking client coverage, D1 hourly distinct anonymous user aggregation coverage, D1 field report source/status/TTL/coarse-radius/no-coordinate/list coverage, comment body privacy/script/URL-spam rejection coverage, comment daily/burst rate-limit coverage, comment/photo moderation report UI wiring, release-gate local Pages report baseline coverage, staging mutation admin-required fail-fast coverage, release-candidate final evidence bundling, production-candidate production read-only smoke wiring, collect-blockers release evidence coverage, release-gate failed-step summary coverage, Cloudflare external-state deployment URL and remote D1 `0002` migration coverage, D1/R2 release evidence operator planning, release-gate D1/R2 read-only evidence ordering, root release harness ledger/status/open-blocker, open-blocker evidence token, UGC moderation runbook section/token guard, Cloudflare cost/usage runbook section/token guard, TestFlight review notes section/token guard, real-device QA ledger guard, public privacy/support page guard, privacy/support URL shape guard, and Next Actions duplicate-line guard coverage, and release ledger refresh. 2026-06-27 `pnpm test -- tests/cloudflare-api.test.ts` passed 125 tests including D1 ranking abuse smoke, UGC moderation runbook guard coverage, Cloudflare cost/usage runbook guard coverage, TestFlight review notes guard coverage, real-device QA ledger guard coverage, public privacy/support page guard coverage, privacy/support URL guard coverage, auth-aware external-state blocker canonicalization, and Worker deployment absence classification. `pnpm cf:external-state` remains `blocked-external` on documented R2, missing Worker deployments, and deployment URL blockers |
| Targeted API tests | pass, 125 tests | 2026-06-27 local run keeps Supabase/Vercel legacy artifact and runtime public URL removal guards, root `release-ledger.yaml`/`RELEASE_STATUS.md` presence, required sections, `docs/current-release-state.md` source-of-truth linkage, open release blocker blocking, open blocker evidence token coverage in `RELEASE_STATUS.md`, duplicated Next Actions operator instruction blocking, UGC moderation runbook required-section/token blocking, Cloudflare cost/usage runbook required-section/token blocking, TestFlight review notes required-section/token blocking, real-device QA ledger required-section/token blocking, public privacy/support page token blocking, privacy/support URL required/HTTPS/no-localhost/no-query blocking, and secret-like value redaction guards, share/OG post Worker-first lookup, configured-Worker no-demo-fallback, no-Worker demo fallback coverage, Worker `/api/posts`, `/api/hashtags`, `/api/questions`, `/api/my-questions` memory fallback and SQLite D1 contract coverage, D1 posts/questions seed idempotency, release-order `0001_initial.sql` -> `0002_posts_questions.sql` migration-chain idempotency, Cloudflare external-state R2-disabled/bucket-missing/deployment-URL/D1-migration/auth-blocker/Worker-deployment classifiers and canonical blocker summary, D1 and R2 release evidence planner non-mutating defaults and production apply confirmation gates, release gate mutating admin-token preflight, staging mutation `--require-admin` fail-fast coverage, `--release-candidate` coverage for final mutating/report/photo/tail evidence, `--production-candidate` coverage for HTTPS production Pages/API URLs and production API/Pages read-only smoke without write smoke, collect-blockers non-mutating guard coverage, failed collect-blockers summary coverage, `/api/places` radius parsing/D1 filtering/client-coordinate non-echo coverage, `/api/rankings` `CACHE` KV miss/hit/TTL/corrupt-entry recovery and enriched item field coverage, D1 ranking abuse smoke for repeated same-user click/like signals, Worker Durable Object WebSocket broadcast coverage, frontend scoped ranking client query coverage for region/area/category/bbox TOP 10 calls, D1 live-surface coverage for same anonymous user click/comment/photo contributing one hourly `unique_user_count` and live-score ordering, D1 `/api/reports` field-report coverage for source `field_report`, coarse verified radius, 3-hour TTL, public listing, `LOCATION_NOT_VERIFIED`, and coordinate/photo URL non-echo, memory fallback field report listing without raw location data, comment body rejection coverage for phone, resident-id, script, and URL-spam patterns without echoing raw body, local Pages report baseline ordering and network artifact redaction guard including `onboarding.dismiss`, coordinate-status preflight, Worker admin token precedence, release gate, Pages smoke hardening, MVP ranking max-limit alignment, comment burst/daily rate limits, and Worker moderation report client coverage under `pnpm test -- tests/domain.test.ts` |
| Browser map/control regression smoke | pass | 2026-06-25 local Pages report smoke against local Next verified the live/fallback map area is visible, initial map tools and current-location button are not covered by bottom nav, bottom nav is opaque so hidden ranking buttons do not look tappable underneath it, `필터`/`교통 켜기`/`이 지역 다시 검색`/header buttons/bottom nav hit-test to their own controls, first-visit onboarding dismisses by real click, traffic toggles state, requery shows toast feedback, the fallback map status banner does not intercept marker hit-tests, and `광안리해수욕장` ranking item plus map marker both open place detail. Screenshots/artifacts: `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-1782366595310.png`, `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-network-1782366595311.json`, `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-console-1782366595312.log` |
| Browser map/detail smoke | pass | Playwright/in-app browser checked 390x844 mobile: Naver auth 401 renders clickable fallback map instead of a blank SDK frame, marker opens detail, filter/traffic/requery/region/bottom nav buttons update state, Next dev indicator no longer covers bottom nav, Seoul empty-region map keeps `표시할 장소 없음` frame, screenshots saved under `/tmp/silsigan-map-clickability-qa-2026-06-19` and Playwright viewport artifacts |
| Browser home-button smoke | pass | Chrome headless CDP against local Next dev: initial `지도` view has a non-empty fallback map surface, `홈` navigation works, top notification button toggles `aria-pressed=true` and toast text, `주차`/`야경` feed tabs update counts and empty state; screenshots `/tmp/silsigan-map-smoke-2026-06-19.png`, `/tmp/silsigan-home-button-smoke-2026-06-19.png` |
| Browser map-bounds fetch smoke | pass | Chrome headless CDP against local Next dev captured Network requests: initial `/api/places?limit=100`, then bounds emission triggered `/api/places?bbox=129.118600%2C35.153200%2C129.311500%2C35.838200&limit=100`; screenshot `/tmp/silsigan-map-bounds-fetch-smoke-2026-06-19.png` |
| Browser place-click Worker smoke | pass | Chrome headless CDP with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4319`: fallback map marker click opened `광안리해수욕장` detail and POSTed `http://127.0.0.1:4319/api/places/busan-gwangalli/click`; screenshot `/tmp/silsigan-place-click-worker-smoke-2026-06-19.png` |
| Browser comment-create Worker smoke | pass | Chrome headless CDP with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4320`: ranking click opened `광안리해수욕장`, comment composer POSTed `http://127.0.0.1:4320/api/comments`, rendered the new comment and `댓글 업데이트` realtime strip; screenshot `/tmp/silsigan-comment-create-worker-smoke-2026-06-19.png` |
| Browser like/report Worker smoke | pass | Playwright with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4321`: Naver auth 401 switched to clickable fallback map without app crash, `광안리해수욕장 상세 열기` opened detail, `좋아요` POSTed `/api/places/busan-gwangalli/like` and showed `좋아요 반영`, `장소 신고` POSTed `/api/moderation/reports` and showed `신고 접수`; artifacts `artifacts/silsigan-like-report-worker-smoke-2026-06-19.png`, `artifacts/silsigan-like-report-worker-network-2026-06-19.json`, `artifacts/silsigan-like-report-console-current-2026-06-19.log` |
| Browser photo-click Worker smoke | pass | Playwright with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4322`: `광안리해수욕장` detail loaded Worker photo via `/api/photos?placeId=busan-gwangalli&limit=12`, clicking `1KB 현장 사진` POSTed `/api/photos/photo_click_smoke_1/click`, updated tile from `클릭 4` to `클릭 5`, and showed `사진 확인이 반영됐습니다.`; artifacts `artifacts/silsigan-photo-click-worker-smoke-2026-06-19.png`, `artifacts/silsigan-photo-click-worker-network-2026-06-19.json`, `artifacts/silsigan-photo-click-console-current-2026-06-19.log` |
| Browser photo-preview Worker smoke | pass | Playwright with `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=http://127.0.0.1:4323`: Worker photo list returned `previewUrl`, browser requested `GET /api/photos/photo_preview_smoke_1/file` for the tile background, tile click sent POST `/api/photos/photo_preview_smoke_1/click`, and count went `2 -> 3`; artifacts `artifacts/silsigan-photo-preview-worker-smoke-2026-06-19.png`, `artifacts/silsigan-photo-preview-worker-network-2026-06-19.json`, `artifacts/silsigan-photo-preview-console-current-2026-06-19.log` |
| Browser my-menu smoke | pass | Chrome headless CDP against local Next dev: `마이` tab visible, `내 제보`, `저장한 게시물`, `차단한 사용자` menu buttons set `aria-pressed=true`, scroll to `내 최근 제보`/`저장한 게시물`/`안전 보호`, screenshot `/tmp/silsigan-my-menu-runtime-2026-06-19.png` |
| Browser admin-filter smoke | pass | Chrome headless CDP against `/admin/moderation/posts`: `차량번호`, `임시 숨김`, `전체` filter buttons set `aria-pressed=true` and update Worker/demo counts and empty/list states, screenshot `/tmp/silsigan-admin-filter-smoke-2026-06-19.png` |
| Browser admin-action smoke | pass | Chrome headless CDP against `/admin/moderation/posts`: demo queue `유지` action posts to admin route, clears the queued item, updates count to `데모 게시물 0건`, and shows completion notice, screenshot `/tmp/silsigan-admin-action-smoke-2026-06-19.png` |
| Admin post moderation input smoke | pass | Local Next dev `POST /api/admin/moderation/posts` returned 400 `VALIDATION_ERROR` for missing `postId` and invalid `action=publish`, while a valid queue `keep` action returned 200 |
| Browser realtime smoke | pass | Chrome headless CDP against mock Worker API: detail shows `Cloudflare DO`/`댓글 업데이트`, POST like and POST moderation report reached API, screenshot `artifacts/realtime-event-visible-smoke-2026-06-19.png` |
| Browser comment/photo report smoke | pass | 2026-06-20 in-app Browser against local Next + mock Worker API: Naver marker rendered, marker click opened detail, Worker photo report opened reason modal and POSTed `/api/moderation/reports` with `targetType=photo`, new Worker comment rendered a comment report button, comment report reason modal POSTed `/api/moderation/reports` with `targetType=comment`, toast and realtime `신고 접수` state were visible. Browser CDP screenshot timed out twice, so evidence is live DOM, console logs, and mock Worker moderation report receipts |
| `pnpm release:status` | blocked-external | 2026-06-26 strict release status passes `docs/current-release-state.md`, root `release-ledger.yaml`, `RELEASE_STATUS.md`, UGC moderation runbook, Cloudflare cost/usage runbook, TestFlight review notes, open blocker evidence token consistency, D1/KV concrete IDs, frontend Cloudflare wrangler config, legacy artifact removal, OpenNext scripts/config, Durable Object/R2/Images binding checks, and Vercel public runtime URL removal; it fails on `release_harness.ledger.open_blockers`, missing `SILSIGAN_PRIVACY_POLICY_URL`/`SILSIGAN_SUPPORT_URL`, plus missing `SILSIGAN_STAGING_PAGES_URL`, `SILSIGAN_STAGING_API_BASE_URL`, `SILSIGAN_PRODUCTION_PAGES_URL`, and `SILSIGAN_PRODUCTION_API_BASE_URL`. With `--cloudflare-external-state-report=/tmp/silsigan-cf-external-state-20260626-after-d1.json`, the same status payload additionally reports `R2_NOT_ENABLED`; production D1 `0002` now passes in the external-state report |
| `pnpm cf:typegen` | pass | 2026-06-20 `wrangler types cloudflare-env.d.ts --env-interface CloudflareEnv --env-file .env.example --include-runtime false` generated Cloudflare env types from tracked sample keys, not local personal `.env.local` keys; generated type files are gitignored |
| `pnpm cf:build` | pass | 2026-06-20 OpenNext Cloudflare build generated `.open-next/worker.js` and `.open-next/assets` for Next.js 16.2.6 with `@opennextjs/cloudflare` 1.19.11 |
| `pnpm cf:web:dry-run*` | pass | 2026-06-20 root frontend `wrangler.jsonc` development/staging/production dry-runs read 89 assets from `.open-next/assets` and validated the `ASSETS` binding without deploying |
| `pnpm cf:external-state` | blocked-external | 2026-06-27 non-mutating Wrangler check after production D1 apply and R2 recheck: OAuth/API-token auth pass, expected R2 bucket names, D1 database names, and API/web Worker names read from config; staging and production Worker dry-run pass, staging and production D1 `0002` evidence pass with `posts=4`, `questions=3`; blockers are now `R2_NOT_ENABLED`, missing configured Worker deployments (`worker_deployment.staging.api`, `worker_deployment.production.api`, `worker_deployment.staging.web`, `worker_deployment.production.web`), and missing staging/production Pages/API deployment URLs |
| `pnpm cf:d1:evidence` | staging pass, production pass | 2026-06-26 production `pnpm cf:d1:evidence -- --env=production --apply --confirm-production --timeout-ms=120000` applied `0002_posts_questions.sql` and seed; follow-up `pnpm cf:d1:evidence -- --env=production --check --timeout-ms=120000` reports no pending migrations, `posts=4`, `questions=3`. 2026-06-25 staging apply/check evidence remains valid with the same counts |
| `pnpm cf:r2:evidence` | pass plan, blocked check | 2026-06-26 `pnpm cf:r2:evidence -- --env=staging --check --timeout-ms=120000` still fails read-only with `cloudflare.r2.enabled` / `R2_NOT_ENABLED`, matching `pnpm cf:external-state`. `pnpm cf:r2:evidence -- --env=production --apply` remains a production bucket creation action and requires `--confirm-production` |
| `pnpm audit --audit-level critical` | pass | 2026-06-24 critical audit reports no known vulnerabilities after transitive overrides for `ws@8.21.0`, `js-yaml@4.2.0`, and `@babel/core@7.29.6` |
| `pnpm smoke:tail-redaction --tail-file=tests/fixtures/redacted-worker-tail.log` | pass | captured-log validator accepts redacted fixture |
| `pnpm smoke:staging` without API URL | fail expected | `SILSIGAN_STAGING_API_BASE_URL=` and `NEXT_PUBLIC_CLOUDFLARE_API_BASE_URL=` returns JSON `ok:false` with `BASE_URL_REQUIRED`; `--tail-only --tail-file=tests/fixtures/redacted-worker-tail.log` still passes without a base URL |
| `pnpm smoke:pages` | pass local mock | 2026-06-20 local Next + mock Worker passed read-only browser smoke with visible map markers, hit-tested traffic/filter/requery buttons, bottom nav home/map transitions, map-marker detail open, Worker `/api/places`, place-scoped `/api/photos?placeId=busan-gwangalli`, marker click `POST /api/places/busan-gwangalli/click`, and `/api/realtime/place/busan-gwangalli`; artifact dir `artifacts/cloudflare-pages-smoke-worker-read-local`. 2026-06-20 mutating browser smoke also passed Worker like/comment/photo UI POST, like/comment cleanup, and required photo click with report intentionally skipped without report opt-in; artifact dir `artifacts/cloudflare-pages-smoke-worker-mutation-local`. 2026-06-22 `pnpm smoke:pages -- --pages-url=http://localhost:3000 --timeout-ms=45000` passed against local Next after the live Naver map/control fix, including new `map.controlsUncovered` coverage that fails if initial `교통`, `이 지역 다시 검색`, or `현재 위치` buttons are covered by the bottom nav; artifacts `artifacts/cloudflare-pages-smoke/pages-smoke-1782102298415.png`, `artifacts/cloudflare-pages-smoke/pages-smoke-network-1782102298417.json`, and `artifacts/cloudflare-pages-smoke-console-1782102298418.log`. 2026-06-25 `pnpm smoke:pages:local-report -- --timeout-ms=60000` starts a local mock Worker + Next dev and drives the Pages browser smoke with `--mutating --report --require-photo --share-post-id=post-local-report-smoke`; latest pass evidence includes `전국 실시간 베타` header copy, visible map surface with interactive marker, visible nationwide/region/map TOP 10 ranking panels, ranking item detail open, map marker detail open without fallback status banner hit-test interception, `map.controlsUncovered`, `layout.bottomNavOpaque`, `onboarding.dismiss`, traffic/filter/requery/current-location hit-test and bottom nav state changes, Worker requests for `/api/places`, `/api/posts`, `/api/hashtags`, `/api/questions`, `/api/my-questions`, `/api/photos?placeId=busan-gwangalli`, realtime room requests for `/api/realtime/place/busan-gwangalli`, `/api/realtime/region/busan`, and `/api/realtime/global`, like/comment/photo click POSTs, place/comment/photo report targetTypes `place`, `comment`, `photo`, share page render `share.postPage`, share OG image `share.opengraphImage`, mock Worker share lookup count `sharedPostRequestCount=2`, screenshot `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-1782366595310.png`, network artifact `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-network-1782366595311.json`, and console artifact `artifacts/cloudflare-pages-smoke-worker-report-local/pages-smoke-console-1782366595312.log`. The local report smoke now fails if the network artifact omits required report target types or stores request `postData`, report note/body, smoke comment text, admin token, anonymousUserId, or original filename, and it also fails if required UI/Worker checks such as `map.controlsUncovered`, `layout.bottomNavOpaque`, `onboarding.dismiss`, `rankings.detail`, `place.detail`, `worker.realtime*`, like/comment/photo click, place/comment/photo reports, or share/OG checks are missing or failed. Latest redaction summary is `eventCount=174`, `reportTargetTypes=comment/photo/place`, `storesPostData=false`, `sensitiveHits=[]`; latest smoke integrity summary is `passedRequiredChecks=31`, `failedChecks=[]`. Earlier hit-test-only local baseline remains under `artifacts/cloudflare-pages-smoke-hit-test-local*` and `artifacts/cloudflare-pages-smoke-control-local*` |
| `pnpm release:gate -- --plan-only` | pass local plan | 2026-06-25 plan-only lists local verify, optional local Pages report baseline, optional Workers tail redaction, strict `release:status`, critical audit, `cf:typegen`, `cf:build`, frontend development/staging/production dry-runs, `cf:preflight`, staging/production `cf:r2:evidence -- --check`, staging/production `cf:d1:evidence -- --check`, `cf:external-state`, API staging/production Wrangler dry-runs, staging Worker smoke, Pages browser smoke, and optional coordinate-status smoke preflight without printing secret values. The plan exposes `SILSIGAN_STAGING_API_BASE_URL` on `staging.api.smoke` and both `SILSIGAN_STAGING_PAGES_URL`/`SILSIGAN_STAGING_API_BASE_URL` on `pages.browser.smoke`, so URL prerequisites are visible before the smoke fails. After production D1 apply, `--collect-blockers` keeps running non-mutating evidence after first failure and normalizes duplicate blocker aliases to `release_harness.ledger.open_blockers`, the four `deployment_url.*` entries, and `R2_NOT_ENABLED`; staging and production D1 evidence both pass. `--release-candidate` requires HTTPS staging Pages/API URLs and final staging evidence flags; `--production-candidate` requires HTTPS production Pages/API URLs and adds production read-only smoke without turning on staging writes |
| `pnpm release:gate -- --skip-verify --skip-dry-run --collect-blockers --tail-file=tests/fixtures/redacted-worker-tail.log --timeout-ms=120000` | blocked-external expected | 2026-06-26 non-mutating blocker collection returned `resultCounts.pass=9` and `resultCounts.fail=7`. Passing steps were tail redaction fixture, critical audit, Cloudflare typegen, OpenNext build, frontend development/staging/production dry-runs, staging D1 evidence, and production D1 evidence with `posts=4`, `questions=3`. Failing steps are strict release status, URL/resource preflight, staging/production R2 evidence, external-state, staging API smoke, and Pages smoke; the top-level blocker summary is normalized to `release_harness.ledger.open_blockers`, missing staging/production Pages/API deployment URLs, and `R2_NOT_ENABLED` |
| `pnpm cf:preflight` | fail by design | D1/KV/resource bindings pass; staging/production Pages/API deployment URLs are not configured |
| `wrangler deploy --env staging` | blocked-external | Cloudflare API returned R2 enablement required for `silsigan-photos-staging` |

## Cloudflare External State

| Item | Current state | Required evidence before release |
| --- | --- | --- |
| Cloudflare web staging deployment | deployed-history, URL missing | 2026-07-10 read-only check finds deployment history for `silsigan-web-staging`; set `SILSIGAN_STAGING_PAGES_URL` and run browser smoke against the selected HTTPS URL |
| Cloudflare API staging deployment | blocked-r2 | Enable R2 in Cloudflare Dashboard, deploy configured API Worker `silsigan-api-staging`, set `SILSIGAN_STAGING_API_BASE_URL`, and run staging API smoke |
| Cloudflare web production deployment | deployed-history, URL missing | 2026-07-10 read-only check finds deployment history for `silsigan-web-production`; set `SILSIGAN_PRODUCTION_PAGES_URL` and run `pnpm release:gate -- --production-candidate` Pages read-only smoke |
| Cloudflare API production deployment | missing | Deploy configured API Worker `silsigan-api-production`, set `SILSIGAN_PRODUCTION_API_BASE_URL=https://...`, and run `pnpm release:gate -- --production-candidate` API read-only smoke |
| Staging D1 database ID | ready-remote, V2 migration blocked | Existing migrations through `0002` remain applied, but the 2026-07-10 read-only check reports `D1_0006_NOT_APPLIED`; review and apply additive `0004`-`0006` to staging before any V2 smoke |
| Staging KV namespace ID | ready-remote | `workers/api/wrangler.jsonc` has concrete `env.staging.kv_namespaces[CACHE].id` |
| Production D1 database ID | ready-remote, V2 migration blocked | Existing migrations through `0002` remain applied, but the 2026-07-10 read-only check reports `D1_0006_NOT_APPLIED`; production apply requires separate approval after staging migration and rollback/read verification |
| Production KV namespace ID | ready-remote | `workers/api/wrangler.jsonc` has concrete `env.production.kv_namespaces[CACHE].id` |
| R2 account enablement | blocked-dashboard | `pnpm cf:external-state`, `wrangler r2 bucket list`, and `pnpm cf:r2:evidence -- --env=staging --check --timeout-ms=120000` return Cloudflare code 10042 / `R2_NOT_ENABLED`. Cloudflare R2 docs say the account needs an R2 subscription added from Dashboard > Storage & databases > R2 > Overview checkout. After that, use `pnpm cf:r2:evidence -- --env=staging --apply` to create any missing configured staging bucket. Production bucket creation requires `--confirm-production` |
| R2/Cloudflare Images/admin staging mutation | not run | Set `SILSIGAN_STAGING_ADMIN_TOKEN`, then run `SILSIGAN_STAGING_MUTATION=1 pnpm smoke:staging -- --require-admin`; coordinate-status smoke additionally requires explicit `SILSIGAN_STAGING_COORDINATE_SMOKE_PLACE_ID`, latitude, and longitude |
| Workers platform tail redaction | not captured | `SILSIGAN_STAGING_TAIL_LOG_FILE=artifacts/cloudflare-tail/staging-tail.log pnpm smoke:tail-redaction` |

## Release Decision

Current state: `blocked-external`

Do not claim production readiness until all of the following are true:

1. `pnpm release:status -- --strict` passes.
2. `pnpm cf:preflight` passes with real staging/production URLs.
3. `pnpm smoke:staging` passes against the staging Worker URL.
4. `SILSIGAN_STAGING_MUTATION=1 pnpm smoke:staging -- --require-admin` passes against staging R2/Images, admin moderation, and user restriction with `SILSIGAN_STAGING_ADMIN_TOKEN` set in the shell.
5. `pnpm smoke:tail-redaction` passes against a real captured Workers tail log.
6. `pnpm smoke:pages` confirms Cloudflare Pages frontend can drive visible map controls, map/detail, Worker read/telemetry paths, and, with explicit mutation flags, comment/photo/report flows through the staging Worker API.
7. `docs/real-device-qa.md` is filled with iPhone and Android device evidence for launch, map, location allow/deny, camera/photo library, comment/photo/like/ranking/report/moderation, redaction, and crash checks.

## Next Actions

1. Review `0004_v2_foundation.sql`, `0005_v2_signals.sql`, and `0006_trust_safety_identity.sql`, back up staging D1, apply them to staging only, then rerun `pnpm cf:d1:evidence -- --env=staging --check --timeout-ms=120000`.
2. Enable R2 in the Cloudflare Dashboard, run `pnpm cf:r2:evidence -- --env=staging --check --timeout-ms=120000`, and create the configured staging bucket only with an approved `--apply` run if it is missing.
3. Confirm NAVER Web Maps service, restrict the client ID to the selected staging/production HTTPS origins, and set `SILSIGAN_NAVER_MAP_WEB_MAPS_CONFIRMED=1` plus `SILSIGAN_NAVER_MAP_ALLOWED_ORIGINS`.
4. Complete source-rights review and attribution text for each source, configure only approved credentials, then enable each source through the audited admin source-policy API. Keep CCTV playback, YouTube, external images, ads, social feed, and live streams disabled.
5. Deploy the staging API Worker, select the existing staging web deployment URL, and set `SILSIGAN_STAGING_API_BASE_URL`, `SILSIGAN_STAGING_PAGES_URL`, `SILSIGAN_PRIVACY_POLICY_URL`, and `SILSIGAN_SUPPORT_URL`.
6. Capture staging read, mutation, admin, R2/Images, Workers tail-redaction, source-ingestion, and NAVER map/fallback evidence. Do not run a release-candidate mutation smoke before the URLs, R2, admin token, and tail file are ready.
7. Generate the Capacitor iOS/Android projects from `apps/webview`, implement the small native settings adapter, configure signing and staging URL/origins, then fill `docs/real-device-qa.md` on both platforms.
8. Record the named moderation/on-call owners and complete legal review for privacy, location, UGC, account deletion, public-source terms, store privacy labels, and final public URLs. Ads remain disabled until the separate M6/M8 approval gate.
9. Apply V2 migrations to production only after staging evidence is clean and a separate production approval is recorded, then deploy production API and select the production web URL.
10. Rerun `pnpm cf:external-state`, `pnpm release:status -- --strict`, and the production-candidate read-only smoke; release remains blocked until every external blocker and real-device row is closed.
